# 핸드오프

마지막 작업: **2026-08-26 (37차 · 피벗 드리프트 · 사용자 결정 4건)** · 다음 작업자(사람 또는 에이전트)가 콜드 스타트로 이어받기 위한 문서.

---

## 30초 오리엔테이션

Yahoo Fantasy NBA **14팀 · $200 옥션 · 로스터 9인 · H2H 13캣** 리그의 드래프트 준비 산출물.

# 🔴 드래프트 **2026-09-05** (사용자 확인 2026-08-26 · 시즌 개막 10-20보다 6.5주 빠름)

문서 곳곳의 "드래프트는 개막 1~2주 전" 가정은 **틀렸습니다.**

| | |
|---|---|
| 선수 DB | 174명 (실측 근거 173 · 루키 3 · 장기부상 제외 1) |
| 코어 플랜 | 7종 + 과열 피벗 7종 = **14개 플랜** (백업은 33차 c7 교체로 소멸) |
| 리그 규칙 | ✅ **지명 시 자동 $1 입찰 있음**(확정) — 태우기 지명은 남이 100% 붙을 선수만 |
| 획득 가능 | **144/174명** (37차 my_max 상향 17명 후 · Haliburton·Adebayo·Harden 신규 진입) |
| 검증 | `python3 validate.py` → **위반 0건** |
| 실전 툴 | `tool/auction-console.html` (오프라인 단일 파일) |
| 리그 내부 실측 | 작년 옥션 120건 전원 식별 (`data/prior_auction_2025_26/`) · 분석 `docs/08` |
| 시장가 | ✅ **실측 기반**(재적합 v2 적용) — 더 이상 저자 추정이 아님 |
| 선수 실측 | ✅ **BBRef 2시즌 GP가중 혼합** **171/174명** (`measured_full.json`) · 최근 시즌 ×1.5 · 13캣 중 12캣 · DD만 공백 |
| 표본 처리 | Kessler(2025-26 **5경기**)처럼 작은 표본은 2024-25가 대부분 반영됨(11%) · 건강한 선수는 최근이 60~72% |
| 상대 기준선 | ✅ **실측** 슬롯당 PTS 18.68 · AST 4.42 · TOV 2.19 (`cores.json.opponent_baseline`) · 총량배분 교차검증 있음 |
| 캣 판정 | ✅ **로스터 9명 전원 · GP 가중** (`cat_team_marginals`) — 21차 정정. 야후는 매일 라인업을 세팅하므로 벤치도 기여한다. 단일 소스 `tool/cat_model.py` |
| 코어 승패 | c1 **10캣** · c2·c3·c5·c6·c7 9 · c4 8 — **전부 승리** (`cat_win_summary`) |
| 승률 판정 | ✅ **주간 맞대결 몬테카를로**(30차) · 상대 6종 · **16000시행**(38차 재실행) · `data/matchup_sim.json` · 목적함수 **maximin**(최저 승률) |
| 최소 승률 | c1 38.7% · c6 36.5% · c7 35.8% · c5 31.4% · c3 31.2% · c4 29.5% · c2 26.3% |
| 주간 경기수 | ✅ **3.299** (`cat_model.GAMES_PER_WEEK` · 38차) — 확정 일정 2026-10-20~2027-04-11 = 174일 = 24.857주 · 82경기. 종전 3.5는 어림이었고 **세 파일에 하드코딩**돼 있었다 |
| 가격 스키마 | **3필드 분리**(35차) — `bid_ceiling`(부를 값) / `expected_cost`(낼 값) / `plan_price`(별칭) |
| 예비비 | `$200 − 계획총액` · 목표 $12 · **7코어 전원 달성**($13~20 · I22) |
| 세팅 변경 | 작년 12팀·로스터10 → 올해 **14팀·로스터9** · 슬롯당 예산 $20.0 → $22.2 |
| 배포판 | https://claude.ai/code/artifact/e75e441d-63ce-4c8c-a504-9f0ced805dca |
| 저장소 | ✅ **git** (2026-08-26 전환 · 36차) · `origin` = github.com/whwnsgml48/nba_fantasy_auction `main` |

**드래프트 중 판단 한 줄**
> 정상 시장: 코어 6 기본 → Hali 할인 시 코어 1 → SGA 할인 시 코어 3 → 앵커 실패 시 코어 4.
> 저가 센터 2명 이상 과열: 즉시 코어 7. Jokić·Sabonis는 조건 충족 시에만 별도 진입.

---

## 28~37차에 바뀐 것 (2026-08-26) — 아래 「남은 작업」보다 이 절이 최신입니다

⚠️ 이 문서의 「남은 작업」 이하 본문은 **27차까지의 기록**입니다. 그 뒤로 감사가 10차례 더
있었고 결론 몇 개가 뒤집혔습니다. 충돌하면 **이 절 → `docs/04-audit-log.md` 28~37차 →
`README.md` → 나머지** 순으로 믿으십시오.

| 차수 | 한 줄 |
|---|---|
| 28차 | M6를 **달러 노출**(`max(dollar_naive, market_high)`)로 재정렬 · 미사용 천장 27건 기계 종결 |
| 29차 | `rel_margin` 비율캣 분모 버그(FG% **10.5배** 부풀림) 정정 → "3%↑ 캣 개수"는 판별력이 없다 |
| 30차 | 🆕 **주간 맞대결 몬테카를로**(`tool/matchup_sim.py`) — 부호 판정 → **승률 판정** |
| 31차 | I21 신설 — 계획가가 시장 상단을 넘는 패딩 126건·$260 노출 (경고 등급) |
| 32차 | 시뮬 산출물을 `data/matchup_sim.json`으로 분리 · 벤치마크 상대 추가 · maximin 목적함수 명문화 |
| 33차 | 재설계 탐색 → **c7 전면 교체(A1)** · c6·c1 단일 슬롯 교체 채택 · KAT 단일실패점 측정 |
| 34차 | **예비비 불변식 I22** 도입 → 7코어 전원 재가격/재구성 · KAT 철수가 $55 |
| 35차 | **가격 3필드 분리**(`bid_ceiling`/`expected_cost`/`plan_price`) · I23 신설 · I21 41건 → **0건** |
| 36차 | **git 저장소 전환** — 초기 커밋 56파일 · 스냅샷은 "git 대체"에서 **커밋 전 필드 diff**로 역할 축소 (아래 7절) |
| 37차 | **`tool/gen_docs06.py` 신설** — docs/06이 "자동 생성됨"이라 적힌 채 생성기가 없어 5차수 낡아 있었다 (아래 8절) |

### 1. 판정 기준이 "이기는 캣 수"에서 **주간 승률**로 바뀌었습니다 (30·32차)

`tool/matchup_sim.py`가 상대 6종(무작위 · 가치최대 · 빅스택 · 가드스택 · **기준선** · **벤치마크**)에
대해 4000시행(seed 20261020)을 돌려 `data/matchup_sim.json`을 냅니다.
목적함수는 **maximin** — 상대 6종 중 **최저** 주간 승률, 동률이면 `p_big5_collapse`
(REB·OREB·BLK·FG%·DD 동시 붕괴 확률)가 낮은 쪽.

⚠️ 파일 `objective.note`에 **"값만 산출한다. 이 지표로 코어를 고르지 않는다(32차)"** 라고
못 박혀 있습니다. 산출은 기계가, 채택은 사람이 합니다.

**같은 3% 마진이 캣에 따라 승률 52~66%입니다** (σ: FT% 5.2% … BLK 21% · DD 62%).
그래서 "N캣 이상 승리" · "3%↑ 캣 개수" 같은 옛 지표는 **서로 다른 화폐를 세는 것**이고,
29차에 실제로 c7 백업안 순위를 뒤집었습니다(채택안이 최소 마진 4.2% 1위 → 0.7% 꼴찌).

검산 앵커: c6 FT% 승률이 상대=기준선에서 **23.5%**(사용자 독립 계산 23.3%) — 통과.
그 과정에서 🔴 **`cat_baselines`의 기준선은 어떤 실제 상대도 아니다**가 드러났습니다 —
지명 풀 126명의 *무제약* 평균인데 실제 상대는 $200·포지션 제약을 받아 싼 빅맨 쪽으로 몰립니다
(BLK +13.2% · OREB +8.0% · STL +6.3% / DD −15.3% · AST −6.5% · PTS −5.9%).
`marginal()` 판정은 캣마다 **다른 방향으로** 편향돼 있습니다.

### 2. 7코어가 전부 재가격되고 5개는 선수도 바뀌었습니다 (33·34차)

| 코어 | 총액 | 예비비 | 캣 | **최소 승률** | 빅5붕괴 | 바뀐 것 |
|---|---|---|---|---|---|---|
| c1 | $182 | $18 | 10 | **38.8%** | 43.5% | 재가격 · BN McConnell → **VJ Edgecombe**(33차) |
| c6 | $181 | $19 | 9 | 36.9% | 40.3% | PG McConnell → **Derrick White**(33차) · SF Knueppel → Edgecombe |
| c7 | $184 | $16 | 9 | 35.2% | 40.4% | **전면 교체**(A1) — 구안은 `cores.json.c7_old` |
| c5 | $180 | $20 | 9 | 32.7% | 42.2% | 재가격 + BN McConnell → **Dyson Daniels** |
| c3 | $183 | $17 | 9 | 31.1% | 49.4% | 재가격 + Mark Williams → **Damian Lillard** |
| c4 | $186 | $14 | 8 | 29.7% | 45.0% | 재가격 + BN McConnell → **Trae Young** |
| c2 | $187 | $13 | 9 | 25.7% | **71.2%** | 재가격 + Drummond → **DeMar DeRozan** |

- **T.J. McConnell을 1순위로 쓰는 코어는 이제 하나도 없습니다** (`docs/05` 8e-2 — 자산이
  아니라 누출이었습니다. 8e는 정정됐습니다).
- **c2가 가장 취약합니다** — 최소 승률 25.7% · 빅5 동시붕괴 71.2%. Jokić 압축의 구조적 대가.
- c2·c3의 앵커(Jokić $88 · SGA $72)는 여전히 `my_max < 시장 하단`이라 **조건부 코어**입니다.
- 신 c7(중가 센터 전환) = KAT · Şengün · Knueppel · Daniels · D.White · Mobley · Gobert ·
  Hart · Okongwu. 빅맨 상한은 A1에 맞춰 $36 → **$130**으로 조정했습니다(옛 $36은 '반센터' 전제의 산물).

### 3. 🔴 우선 0(c7) 유지 — 33차의 "c6로 옮기자" 권고는 34차에 **철회**됐습니다

33차는 발동 세계(과열빅 상대)에서 c6 76.8% vs c7 76.7% 동률이니 c6를 우선 0으로 올리자고
제안했습니다. 34차에 **실행 가능성 제약**(저가 센터 6명을 `overheat_at` 이상으로 강제 매수)을
걸어 재계산하니:

| 계획 | 강제 총액 | 예비비 | 실행 |
|---|---|---|---|
| c1 | $203 | −3 | **불가** |
| c6 | $209 | −9 | **불가** |
| **c7 (A1)** | **$189** | $11 | **가능** (LCC 노출이 Gobert 1명뿐) |

**"승률이 높다"와 "그 로스터를 살 수 있다"는 다른 질문입니다.**
76.8%는 조립 불가능한 로스터의 승률이었습니다. 승률표를 읽을 때 **실행 가능성을 먼저 볼 것.**

### 4. 가격은 세 필드입니다 (35차) — 섞으면 안 됩니다

사용자 지적 *"코어플랜 보니까 가격이 말이 안 되더라"* 에서 출발했습니다
(McConnell 계획 $8 / 시장 $1-3 = 2.7배 — 총액 하한 $180을 맞추려 오차가 가장 작은
$1-3 구간에 남는 돈을 얹은 결과).

| 필드 | 계산 | 뜻 |
|---|---|---|
| `bid_ceiling` | `min(my_max, 단일상한, 철수가)` | **부를 최대치** — 불변식 1 · 앵커 여유도 |
| `expected_cost` | `clamp(시장중간, ·, bid_ceiling)` | **예산 계산** — 총액 · 예비비 · 빅맨 상한 |
| `plan_price` | `expected_cost` 별칭 | 툴·기존 검사 하위 호환 |

하한 규칙도 함께 바뀌었습니다:

    (구) 총액 < $180 → 위반
    (신) 예비비 < $4 → 위반 · < $8 → 경고 · > $25 → 과소 편성 경고 · 총액 > $200 → 위반

예비비 목표는 $12이고 **7코어 전원 달성**($13~20)입니다. 근거는 "앵커 1명이 시장 상단으로
가면 예산이 넘는다"입니다. **I21(계획가 > 시장 상단)은 41건·$99 → 0건**이 됐습니다.

### 5. 🔴 남은 최대 병목 — **피벗 로스터가 예산을 못 씁니다**

base는 정리됐지만 피벗은 그대로입니다. 과소 편성 경고 5건:
**c6 피벗 $149/예비 $51** · c4 $165/$35 · c5 $168/$32 · c1 $171/$29 · c2 $173/$27.
c6 피벗은 **전원 시장 상단에 사도 $171**입니다.

34차 부록에서 base와 같은 재가격 규칙을 피벗에 적용해봤고 I21은 41건 → 2건으로 줄었지만
**7개 피벗 전부 하한 미달**이 되어 스냅샷에서 되돌렸습니다. 대체 낙폭 $21~34를 흡수하려면
base가 $201~214여야 하는데 예산 $200에서 불가능합니다.
→ **해결은 가격 조정이 아니라 피벗 로스터 재설계입니다** (base에서 c4·c5에 한 작업과 같은 것).
기록: `docs/05` 2b-5.

### 6. KAT 단일실패점 · 철수가 $55의 근거 (33·34차)

c1·c6·c7이 전부 KAT에 의존합니다. KAT 제외 제약으로 maximin 재탐색:

    KAT 보유 최고 (c1)        38.5%
    KAT 제외 최고 (noKAT-N1)  35.1%   → 손실 3.3%p (신 c6 기준 2.4%p)

`noKAT-N1` = Şengün · Amen Thompson · Mobley · Duren · D.White · Edgecombe · Clingan · Daniels · Bane
(`cores.json.kat_single_point`).

철수가 $55는 **승률이 아니라 예산이 정했습니다.** 가격이 로스터를 바꾸지 않는 구간에서는
승률이 수학적으로 동일하므로($49~$58 전부 같은 9명), 구속하는 것은 I22입니다 —
KAT 제외 8칸 원가 $141 → `p ≤ $47`이면 예비비 목표, `p ≤ $51` 경고선, **`p ≤ $55` 위반선**.
`overheat_thresholds`에 `tier=anchor`로 들어가 있습니다(anchor 계층은 `overheat_at`이 null).

"$49→33.7% · $55→35.8% · $61→35.2%" 같은 측정 차이는 **1200시행 노이즈(±1.3%p)** 입니다.

### 7. git 저장소가 됐습니다 (36차 · 2026-08-26)

`origin` = https://github.com/whwnsgml48/nba_fantasy_auction · `main` ·
초기 커밋 56파일. 푸시 전 `ls-remote`로 원격이 빈 저장소임을 확인했습니다.

**`snapshot_data.py`는 계속 씁니다.** git이 있어도 `git diff`는 `players.json`
(indent=1 · 27,987행)에서 한 필드 변경에 수백 줄을 뱉고 배열 재정렬을 전부 변경으로
잡습니다 — 4차에 `diff -r`을 버린 이유와 같습니다. 역할만 나뉘었습니다.

| 층 | 수단 | 역할 |
|---|---|---|
| 영구 이력 | `git` | 커밋 단위 되돌리기 · 원격 백업 · 차수 간 비교 |
| 작업 중 | `tool/snapshot_data.py` | 필드 단위 변경 요약 · **커밋 전 셀프 리뷰** |

바뀐 규칙 하나: scratchpad 스냅샷은 세션 전용이므로 **세션이 끝나기 전에 커밋하십시오**
(2a-1). 23차 사고의 근거였던 "scratchpad가 사라지면 되돌릴 수 없다"가 커밋으로 해소됩니다.

**저장소는 public 유지입니다** — 사용자 결정(2026-08-26): *"어차피 뭐 누가 본다고."*
`cores.json`에 선수별 철수 가격·입찰 상한과 태우기 지명 순서가 전부 들어 있으므로,
같은 리그 참가자가 URL을 알면 전략이 노출된다는 점만 알고 계십시오. **재론 불필요** —
다시 제안하지 말 것.

### 8. docs/06이 "자동 생성됨"인 채로 5차수 낡아 있었습니다 (37차 · 2026-08-26)

README와 이 문서 둘 다 *"docs/06은 cores.json에서 전체 재생성된다"* 고 적어놨는데
**`gen_docs06.py`가 없었습니다.** 딱지가 붙으니 아무도 읽지 않았고, 드래프트 당일
참조 문서가 c7을 옛 '반센터'로, 코어 총액 7개 중 6개를 틀리게, McConnell을 21회
싣고 있었습니다. **`tool/gen_docs06.py`를 신설**해 전량 생성으로 바꿨습니다(624줄 · 멱등).
문서에 없던 **주간 승률 표 · 예비비 · 가격 3필드**가 이제 들어갑니다.
함께 정정: `docs/02` 폐기 곡선 표시 · **"15개 플랜" → 14개**(백업은 33차에 소멸).

🔴 **재생성이 데이터 문제 2건을 드러냈습니다** (미수정):
- **c6 피벗의 PG가 아직 `T.J. McConnell $2`** 입니다 — 33차에 base만 `Derrick White $39`로
  올리고 피벗은 손대지 않았습니다. c6 피벗 예비비 $51(최악)의 직접 원인이고,
  「피벗 로스터 재설계」의 가장 구체적인 착수점입니다.
- **c6 `premise`가 낡았습니다** — "A/T를 McConnell + DeRozan으로 분산 조달"인데
  현재 공급원은 `Derrick White` + DeRozan입니다. 생성기가 `premise`를 그대로 실으므로
  **데이터를 고쳐야 문서가 고쳐집니다.**

✅ **발행 아티팩트는 최신입니다** — 발행본과 로컬 본문 156,649자가 완전 일치합니다.
재발행 불필요(2a-0 규칙 자체는 유효).

---

## 이어받을 때 먼저 할 것

```bash
cd ~/personal_work/nba_fantasy_auction_2026
git status                   # 작업트리 청결 확인 (36차부터 git 저장소)
python3 validate.py          # 14개 플랜 무결성. 위반 0건이어야 정상
open tool/auction-console.html
```

읽는 순서: `README.md` → **이 문서의 「28~36차에 바뀐 것」** → `docs/04-audit-log.md` 28~36차
→ `docs/05-limitations.md`(한계) → `docs/06-cores.md`(판단표·코어·피벗)

승률 숫자가 필요하면 `data/matchup_sim.json`(코어별 상대 6종 · 캣별 승률 · 빅5붕괴 확률),
재설계 후보 이력은 `data/rebuild_candidates.json`을 보십시오.

---

## 남은 작업 (우선순위)

### 🔴 지금 열려 있는 것 (2026-08-26 · 37차 종료 시점)

이 표가 현재 상태입니다. **아래 번호 항목(0~6번)은 27차까지의 기록**이고 대부분 해소됐습니다.

| # | 항목 | 근거 |
|---|---|---|
| 1 | **피벗 로스터 재설계** — 7개 피벗이 예산을 못 쓴다(과소 편성 경고 5건 · c6 피벗 예비 $51). 재가격으로는 불가, 34차에 시도하고 되돌림. **착수점: c6 피벗 PG가 아직 McConnell $2**(33차 미반영) | 위 5·8절 · `docs/05` 2b-5 |
| 2 | **치환필요 17건** — Haliburton(c1·c5) · Jokić(c2) · SGA(c3)가 `my_max < market_low`. 대체후보로 실행하면 총액이 $157~171로 떨어져 다시 과소 편성 | `validate.py` 상단 표 |
| 3 | **KAT 단일실패점** — c1·c6·c7이 전부 의존 · 제외 시 maximin −3.3%p | 위 6절 · `cores.json.kat_single_point` |
| 4 | **c2 빅5 동시붕괴 71.2%** — 7코어 중 최악. 재설계 대상 후보 | `data/matchup_sim.json` |
| 5 | **상향 17명 미적용**(KAT 62→71 · D.White 44→52 · Şengün 46→52 등) — 하향은 규율 강화지만 상향은 실지출이라 리스크 비대칭. 사람 판단 대기 | 23차 · `value_reference.gp_adjusted_my_max` |
| 6 | **경계 감시 14명**(`\|div\| 18~22`) — 위반은 아니지만 한 번 더 흔들리면 M5/M6로 들어옵니다 | `validate.py` · `data/divergence_state.json` |
| 7 | 🟡 **Haliburton 철수가 $50 미결** — my_max는 $56인데 철수가가 $50에 묶여 계획가가 시장하단 $54 미만. 올리면 c1 $188(예비 $12)·c5 $186으로 **c1이 실행 가능해짐**. 입찰 결정이라 보류 | 37차 |
| 8 | **야후 실제 포지션 자격 확인** — 남은 유일한 외부 확인 항목 (자동 $1 입찰·Sabonis·Hali는 37차에 확정/판단 완료) | — |

**손대면 안 되는 것**: `cat_team_marginals`·`cat_win_summary`(캣 판정)와
`matchup_sim`(승률 판정)은 **별개 지표**입니다. 30차에 시뮬은 전자를 건드리지 않는다는
원칙으로 만들어졌습니다. 하나를 다른 하나로 덮지 마십시오.

---

### 0. ✅ 실측 보정 (2026-08-20 전부 완료)
**2026-08-20: 작년 옥션 실측을 확보했습니다** (`data/prior_auction_2025_26/`, 12팀×10명=120건,
검산 통과). 분석 전문은 `docs/08-prior-auction-calibration.md`.

결정해야 할 것 3가지 — 전부 코어 재설계를 유발하므로 사람 판단 필요:

1. ~~**과열 임계값 재설계**~~ → ✅ **완료 (2026-08-20, 10차 감사)**
   `threshold` 한 필드가 **철수 가격**과 **시장 과열 신호** 두 개념을 겸하던 것을 분리.
   `overheat_at`(실측 기대치 기반) 신설 + `tier` 3종(`name_big`/`low_cost_center`/`anchor`).
   코어 7 판정은 `low_cost_center` 계층의 `overheat_at`만 셉니다(4명 → 6명, 네임밸류 빅 제외).
   **백테스트: 작년 실측 대입 시 3명 발동 → 0명 발동.** 철수 가격은 한 건도 변경 없음
   (기존 피벗·백업 조건 전부 유효). 검증기 불변식 6종 + 툴 임베드 드리프트 검사 추가.
   상세: `docs/04-audit-log.md` 10차 · `docs/06-cores.md` 과열 임계값 · 2계층.
2. ~~**앵커 여유 $0**~~ → ✅ **완료 (2026-08-20, 11차 감사)**
   문서가 5명으로 적었지만 실제로는 앵커 14개 중 여유 $0이 **9개**였고 원인이 둘이었습니다.
   ① **명목 여유가 허구** — 예산 여유가 없으면 더 부를 수 없음(명목 합이 실효 합보다 $28 큼).
   비앵커 계획가를 재적합 근거로 트림해 여유 확보 → **`constraint: budget` 0건**.
   ② **피벗·백업에 대체후보가 아예 없었음** — 획득 불가 경로 6건 방치. 이중세계 유효
   대체 주입. 그리고 `on_fail` 3종 구조화(`substitute`/`switch_core`/`pivot`).
   **재적합 적용 시 위반 22건 → 4건**, 남은 4건은 c2·c3의 선언된 조건부 베팅.
   **c6(기본값)·c7(우선 0) 모두 실측 곡선에서 실행됩니다.**
   상세: `docs/04-audit-log.md` 11차 · `docs/06-cores.md` 앵커 여유 정책.
3. ~~**재적합 적용**~~ → ✅ **완료 (2026-08-20, 12차 감사)**
   `market_low`/`market_high`가 이제 **실측 기반**입니다(156/174명 변경).
   상위 126명 합 $2,914 → **$2,772** · 획득가능 139 → **141명** · 잉여(+) 94 → **122명**.
   **깨진 코어 0개** — `validate.py` 위반 0건, c2·c3만 조건부 베팅으로 선언.
   ⚠️ **결론 하나가 뒤집혔습니다**: 잉여 상위 20 중 **12명이 C 자격**.
   기본 경로는 "이름값 없는 센터를 싸게 쓸어담기"(코어 6·1)이고 코어 7은 그게 막혔을 때의 보험.
   상세: `docs/04-audit-log.md` 12차 · `docs/08` 5절.

### 🎉 0번 3항 전부 완료 — ~~남은 최상위 병목은 2번(26위 이하 데이터)~~
> ⛔ **이 줄은 낡았습니다.** 2번은 14차에 해소됐고, 현재 최상위 병목은
> **피벗 로스터 재설계**입니다 — 위 「지금 열려 있는 것」 참조.

### 1. 시장 예상가 검증 🟡 (부분 해소)
`market_low`/`market_high`는 여전히 저자 추정이지만, 이제 **곡선 형태를 실측과 대조**했습니다.
- **상단을 $11~16 싸게 봤음** — 1위 재적합 $97 vs 우리 추정 $84, 5위 $83 vs $67
- **중하단은 비싸게 봤음** — 실측은 낙찰 인원의 1/4($5 이하 32명)을 최저가로 채우는데
  우리 모델은 9명뿐. 그 인원을 $11-20에 몰아놨음(36명 vs 실측 23)
- 순 총액은 여전히 $151 초과 → 실전에서 **중하단에서 돈이 남아 상단 초과를 회수**하는 구조
- 엔드게임이 무름: $1 낙찰 13명, 리그 $32 미소진 → **마지막 2~3슬롯에 예산 남길 필요 없음**
- 세팅 변경이 원인: 돈 +16.7%인데 낙찰 인원 +5%뿐 → 금액 스케일 **1.117**

- **드래프트 당일**: 초반 5~10명 실낙찰가를 툴 `시장가 보정`에 입력 → `보정 적용` 토글
- 교차검증 도구: https://hashtagbasketball.com/fantasy-basketball-auction-values
  (14팀 · 9로스터 · 13캣 입력)

### 1.5 `cat_weights` 검증 ✅ (14차에서 전체 스탯으로 완료)
**2번에 종속된다고 봤지만 상당 부분은 독립이었습니다.** 26위 이하 없이 증명 가능한 근거 3종:
`A/T 리더보드`(top-25가 아니라 **자격자 전수 25명**) · `player_lines`(34명 13캣 전부 실측) ·
`리더보드 내부 단조성`(둘 다 관측되면 역전은 26위 이하와 무관하게 모순).

✅ **완료**: 규칙 3종(R1 엘리트 하한 · R2 단조성 · R3 음수기여) 도입, **수정 26건(22명)**,
수정 후 **위반 0건**. `validate.py`가 상시 검사합니다(음성 테스트 3종 확인).
**A/T는 완전 검증** — w≥3 경계 오분류 0건, `at_marginal_lift` 기재값 25명 전부 실계산 일치.

🔴 **함정 기록**: "w3인데 top-25 밖 36명"을 전부 과대로 판정했다가 **틀렸습니다.**
Giannis는 실측 27.6 PPG인데 PTS 리더보드에 없습니다(36경기 → 출장 자격 필터).
**리더보드 부재 ≠ 저생산.** 실측을 1차 근거로 재분류하니 증명된 과대는 **5명**뿐이었습니다.

✅ **14차에서 전체 스탯으로 재검증** — 커버리지 캣당 25명 → **36~74명(97~100%)**.
수정 **111건**(재배정 109 + 음수기여 제거 2), 검증기 규칙을 M1·M2·M3으로 교체, 전부 0건.
`Curry 3P%` w1→w3가 대표 사례 — 기존 리더보드가 **비율순**이라 39.3% on 11.3시도를
하한 40.6%에서 잘라냈고 그 부재가 "엘리트 아님"으로 굳어 있었습니다(패턴 ③).

🟡 **남은 것**
- **DD** — BBRef 미집계. 36명 중 25명만 검증 (**2b번**)
- **GP<40 24명** — 가중치가 데이터 미검증(표기됨). 역할 추정에 의존
- ~~**PTS 엘리트 기준 재정의**~~ → ✅ **완료 (17차)** — w3 41명(24%) → **23명(13%)** ·
  PTS 보유 92 → 62명. 한계기여 통일 정의(기준선 18.68 · 임계 w3≥24.9 · w2≥20.1).
  🔴 **더 심각한 문제를 함께 발견**: `w2`(플러스) 37명 중 **29명이 한계기여 음수**였습니다
  — "플러스"가 실제로는 캣을 깎고 있었습니다.
  🔴 **7개 코어 전부 PTS 기준선 미달**(기준선 3안 모두). c2·c3·c7의 "PTS 목표" 선언은
  거짓이어서 포기로 정정했습니다. c7 백업만 유일하게 확보(+7.1).
  → **`PTS 포기 선언인데 규칙상 확보` 모순이 c1·c4·c5·c6에서 사라졌습니다** (규칙이 틀렸음)
- ~~**REB·3PM·AST·STL의 `w2` 음수 기여**~~ → ✅ **완료 (18차)** — 통일 정의를 전 캣에 적용.
  **변경 352건** · w3 몫이 전 캣 9~16%로 정렬 · 한계기여 ≤0 가중치 0건.
  비율 캣 기준점도 리그 583명 → **지명 풀(MPG≥25)** 로 상향(FG% 46.7%→48.5% 등)
- ~~**`TOV` 가중치 체계 부재**~~ → ✅ **완료 (16차)** — 15명 → **70명**(w3 23·w2 25·w1 22).
  A/T와 같은 한계기여 모델. c1·c5·c6·c7의 TOV 목표 선언이 모두 충족됐습니다.
  ⚠️ **판정은 가중치 합이 아니라 `tov_team_marginal`로 합니다** — 가중치는 1~3 양수뿐이라
  `Jokić 3.7 TOV`처럼 흘리는 선수를 음수로 표현하지 못합니다(가중치 합 3인 c1이 합 6인 c2보다
  실측이 더 좋음). 상세: `docs/07` TOV 절 · `docs/04` 16차

### 1.3 🆕 반복 작업은 스크립트로 (19차)
```bash
V=../nba_2026/.venv/bin/python      # 이 프로젝트엔 venv가 없어 재사용
$V tool/recompute_cores.py          # cores.json 파생 필드 전부 재계산
$V tool/sync_tool.py                # 툴 HTML 임베드 상수 7종 재생성
python3 validate.py                 # 위반 0건이어야 정상
```
**계획가·로스터를 하나라도 고치면 위 3개를 순서대로 돌릴 것.** 안 돌리면 exit 1입니다.
데이터 수집은 `tool/fetch_bbref.py` → `tool/build_measured.py`.

### 1.4 ~~c4는 주간 승리를 못 한다~~ → ✅ **해결 (19차)**
`UTIL Gobert $16 → Immanuel Quickley $5` — 4번째 빅 대신 가드.
**6캣 ❌ → 9캣 ✅** (AST·STL·A/T 뒤집힘). 총액 $194 유지, 빅맨 $105→$92.
피벗도 Gobert 의존 제거로 단순화(`Mobley > $30 → Kessler` 한 건).

**c6도 함께 교정**: `Knueppel $25`+`Daniels $15`를 벤치에 파킹하던 것을
`Clingan`·`Gobert` 벤치로 바꿔 **7→8캣 · 벤치 $40→$17**(선수 교체 0 · 비용 $0).
c6 피벗도 `Merrill $6`↔`Murphy $32` 교환으로 **6→7캣 · 벤치 $38→$12**.

**→ 7개 코어 전부 주간 승리**: c1·c4 9캣 · c2·c3·c6 8캣 · c5·c7 7캣.

⚠️ **최적화 함정 2종을 기록해둡니다**
1. 평가기는 선발만 계산하므로 **비싼 선수를 벤치로 보내면 캣이 좋아집니다.**
   전수 탐색이 앵커 KAT $50 벤치(9캣)를 제안했습니다 — 예산 25% 폐기입니다.
   **벤치 지출 ≤$20 · 앵커 벤치 금지** 제약을 반드시 걸 것.
2. 절감분 재배치가 **철수가를 넘으면 플랜이 자기 피벗을 트리거**합니다(Mobley $31 > 철수 $30).
   `recompute_cores.py`가 `ceil_price=min(my_max, 단일상한, 철수가)`로 차단합니다.

### 1.5 ~~c7 피벗·백업이 6캣~~ → ✅ **해결 (20차)** · 15개 플랜 전부 7캣 이상

**피벗**: PG 1순위가 `Haliburton $50`(획득 불가)이라 명목 6캣이었지만,
대체 `Derrick White $44`로 실행하면 7캣이었습니다. **명목 로스터가 실행 불가 선수를
1순위로 두면 평가가 거짓이 됩니다** → White를 1순위로 승격, Hali는 c7에서 제거.

**백업 조건도 낡아 있었습니다**: `JJ AND Hali`였는데 11차에 대체후보를 넣어 Hali가 막혀도
피벗이 실행됩니다 → `JJ > $58 AND Derrick White > $44`로 교체
(`Derrick White`를 `overheat_thresholds`에 `tier: anchor`로 추가).

**백업 로스터**: `Mitchell $48`(획득 불가) 의존을 끊고 `Daniels · Garland`로 교체.
**7캣을 전부 4.2% 이상 마진으로** 이깁니다. 9캣 옵션이 있었지만 4캣이 0.1~1.9%로 취약해
버렸습니다 — **캣 수보다 마진**입니다.

| 코어 | 우선 | 본체 | 피벗 | 백업 |
|---|---|---|---|---|
| c1 | 1 | 9 | 8 | — |
| c2·c3 | 조건부·3 | 8 | 9 | — |
| c4 | 4 | 9 | 8 | — |
| c6 | 2 (기본값) | 8 | 7 | — |
| c5·c7 | 격리·0 | 7 | 7 | 7 |

### 1.2 🔴🔴 21차 모델 정정 — 반드시 먼저 읽을 것
**16~20차는 "야후 H2H는 선발만 집계"라는 틀린 전제로 계산했습니다.**
야후는 라인업을 **매일** 세팅하고 NBA 팀들은 서로 다른 날에 경기합니다.

```
로스터 9명 × 주 3.5경기 = 31.5 선수-경기
선발 7슬롯 × 7일        = 49 슬롯-일   → 용량 남음
```
→ **9명 전원 집계.** `slot`/`BN` 표기는 명목이고 캣 기여와 무관합니다.
주간 기여 ∝ 경기당 스탯 × 출장 경기수이므로 **GP/82 가중**을 적용합니다.

**두 기준선을 분리했습니다** (같은 실수 반복 방지):
| 용도 | 필드 | 이유 |
|---|---|---|
| 선수 가중치 | `baseline_per_game` (경기당·비가중) | "뛸 때의 실력". 출장 리스크는 `GP`·`flag`가 담당 |
| 팀 한계기여 | `baseline` (GP 가중) | "실제 누적" |

**되돌린 것**: c4의 19차 변경(`Gobert → Quickley`) — Gobert의 BLK 1.6이 BLK 캣을 지탱해
정정 모델에서 9→8캣 악화였습니다. "4번째 빅이 낭비"라는 진단은 선발-only 전제의 산물입니다.
**c6 벤치 재배정은 캣 영향 0**이고, "벤치 $40 파킹은 낭비"라는 19차 지적도 틀렸습니다.
**c7 변경은 유지** — 획득 불가 선수 의존을 끊은 것이라 모델과 무관하게 옳습니다.

⚠️ **`validate.py`는 내부 정합성만 봅니다. 규칙 자체가 틀렸는지는 못 잡습니다.**
모델 가정을 바꿀 때는 `tool/cat_model.py`의 주석(용량 검산·기준선 목적)을 먼저 읽으세요.

### 1.2b 🔴 "리더보드에 없다"를 "못한다"로 읽지 말 것 (22차 · 세 번째 재발)
출처(StatMuse·BBRef 리더보드)는 **출장 자격 필터**가 있습니다. 저출장 선수는
수치가 리그 1위급이어도 명단에 없습니다. 이 부재가 "엘리트 아님"으로 굳은 사례:

| 차수 | 선수 | 잘못된 판정 | 실제 |
|---|---|---|---|
| 13차 | Giannis | PTS top-25 밖 | 27.6 PPG (36경기) |
| 14차 | Curry | 3PT% 엘리트 아님 | 39.3% on 11.3시도 (레버리지 상위) |
| 14차 | Hartenstein | OREB 엘리트 아님 → 매수 철회 | OREB 3.2 (하한 2.4) |
| **22차** | **Kessler** | **BLK 엘리트로 오분류** | **BLK 2.4(리그 3위급) · 2025-26 5경기** |
| 22차 | A.Davis · Porziņģis · Claxton · Irving · Eason | 같은 유형 | 전부 엘리트 |

**대응**: 실측을 2시즌 GP 가중 혼합으로 바꿨고(`tool/build_measured.py`),
`validate.py` **M4**가 서술문↔가중치 충돌을 상시 검사합니다.

### 1.5b 🟡 명목 로스터 vs 실행 라인업 (20차 발견)
**캣 평가는 "실제로 코트에 서는 7명"으로 해야 합니다.** 두 유형을 만났습니다.
1. 능력자를 **벤치**에 둬서 기여가 0으로 계산됨 (야후 H2H는 선발만 집계) — 19차
2. **획득 불가** 선수를 1순위에 둬서 평가가 거짓 — 20차

`cat_team_marginals`는 여전히 **명목 로스터** 기준입니다.
`△ 치환필요`는 **현재 17건**(c1·c2·c3·c5 — Haliburton·Jokić·SGA)이고 **실행 캣 수는
여전히 미계산**입니다. 33차에 c1의 조건부 성격을 판단표 `cond.feasibility`에
명시(**"Haliburton 실낙찰가 ≤ $50"** · `on_fail: c6`)한 것이 부분 대응입니다.

### 1.4b (구) c4 분석
H2H 13캣은 **7캣**을 이겨야 주간 승리입니다. 선발 7명의 실측 팀 한계기여로 계산한 결과:

| 코어 | 판단표 우선 | 승리 캣(DD 포함) | 판정 |
|---|---|---|---|
| **c1** | 1 | **9** | ✅ 최강 |
| c2 · c3 | 조건부 · 3 | 8 | ✅ |
| c5 · **c6** · c7 | 격리 · **2(기본값)** · 0 | 7 | ✅ 승리선 딱 걸침 |
| **c4** | **4** | **6** | ❌ **패배** |

- **c4는 앵커 실패 시 가는 안전망인데 지는 플랜입니다.** 재설계 필요.
- **c6(기본값)은 7캣으로 여유가 없습니다.** 한 캣만 밀려도 패배.
- c1이 9캣으로 가장 강하지만 Haliburton 확보가 조건이고 그는 실측 곡선에서
  획득 불가(치환 필요 — 1.6번 참조).

**코어 다양성 문제도 드러났습니다**: c1~c6이 사실상 같은 빅맨 캣 조합
(REB·OREB·BLK·FG%·TOV·A/T·DD 승 / PTS·FT%·3PM·3P%·STL 패). c7만 정반대.
상대가 빅맨 캣을 맞불로 가져오면 6개 코어가 동시에 흔들립니다.

승패 상세는 `cores.json`의 `cat_win_summary` · `cat_team_marginals`에 있고
`validate.py`가 선언 일치를 상시 검사합니다.

### 1.6 ✅ `my_max` 재산정 — **해소 (23차)**
22차까지 `cat_weights`는 계속 고쳤지만 `my_max`는 한 번도 재산정하지 않아
값이 전부 옛 프로필 기준이었습니다. 23차에 독립 평가 모델(`tool/value_model.py`,
z-score 12캣 · GP 가중)을 붙여 대조하고 **하향만 적용**했습니다.

**적용 규칙**: 혼합 GP < 55 이고 `my_max × (GP/64.7)` 가 현재보다 $4 이상 낮으면 하향.
근거는 모델이 아니라 규칙입니다 — 로스터 9명 · IL+ 미지명 · 라인업 매일 설정이므로
시즌 기여는 경기수에 비례합니다.

| 선수 | 이전 | 보정 | 혼합 GP | 시장 |
|---|---|---|---|---|
| Ja Morant | $28 | **$17** | 38.8 | $32-40 |
| Lauri Markkanen | $28 | **$19** | 44.1 | $28-34 |
| Paul George | $14 | **$8** | 38.7 | $6-12 |
| Jimmy Butler III | $20 | **$14** | 46.3 | $12-20 |
| Anthony Davis | $16 | **$10** | 39.5 | $19-25 |
| Kristaps Porziņģis | $12 | **$7** | 36.7 | $7-13 |
| Giannis Antetokounmpo | $30 | **$25** | 53.2 | $61-69 |
| Dejounte Murray | $8 | **$3** | 24.1 | $4-10 |
| Walker Kessler | $18 | **$14** | 51.9 | $13-19 |
| Mitchell Robinson | $24 | **$20** | 53.2 | $1-9 |

전원 `my_max_basis`(이전값·규칙·이유) 기록. 계획가 6곳도 함께 조정했습니다
(Kessler $18→$14 ×4 · Paul George $13→$8 · Porziņģis $12→$7).
셋 다 **연속 두 시즌 결장**입니다. Kessler는 22차에 올렸다가 이번에 내렸는데 축이 다릅니다 —
**뛸 때의 생산성은 그대로 4엘리트(OREB3 FG%3 REB3 BLK3), 깎인 것은 출장 기대치**입니다.

🟡 **상향 17명은 미적용 · 사람 판단 대기**
KAT 62→71 · D.White 44→52 · Knueppel 26→33 · Amen 42→49 · Şengün 46→52 ·
Duren 46→52 · NAW 22→27 · Bane 26→31 · Gobert 26→30 등(전부 GP 70+).
근거 없이 올리는 것이 오류 패턴 ①이고, 하향은 규율 강화지만 상향은 실제로 돈이
나가는 방향이라 리스크가 비대칭입니다. `value_reference.gp_adjusted_my_max`로 조회 가능.

**참고선 `value_reference`(171명)** — `z_total`·`z_by_cat`·`dollar_naive`·
`rank_by_value`·`rank_by_my_max`·`rank_divergence`·`gp_adjusted_my_max`.
⚠️ **달러 비교는 무효**입니다(z 합산이 상단을 압축 — Jokić $53 vs 시장 $93).
**순위 대조용**입니다. DD 미포함이라 빅맨은 소폭 과소.

⚠️ **부수 발견**: 8차 시장 재적합은 *순위 보존* 재적합이었습니다.
`market_low/high`의 **금액은 실측이지만 순서는 여전히 우리 판단**입니다
(Vučević z순위 18위인데 시장가 $1-3).

### 1.7 🆕 폴백 시즌 3명 주의 🟡 (15차)
Haliburton(73경기)·Kyrie Irving(50경기)·Damian Lillard(58경기)는 2025-26 전체 결장이라
**가중치 근거가 2024-25**입니다. 세 명 `flag`에 명시했습니다. Lillard는 엘리트 캣이
+3으로 가장 크게 올랐는데 그 근거가 부상 전 시즌이라는 점을 함께 읽어야 합니다.

🟡 **부수 발견: `targeted_cats`가 규칙과 어긋남 (7개 코어 전부)**
`README`의 "팀 가중치 합 ≥ 6 = 확보" 규칙으로 계산하면 c1·c4·c5·c6은 **PTS를 포기 선언했는데
규칙상 확보**(합 9~13), c7은 **FG% 포기 선언인데 합 6**입니다.
원인은 코어가 아니라 **PTS 엘리트 기준이 느슨해 규칙이 무의미**해진 것입니다.
선언값을 규칙값으로 덮지 않았고, `cores.json`에 `cat_weight_sums`·`targeted_cats_by_rule`·
`rule_divergence`를 데이터로 남겼습니다. 상세: `docs/04-audit-log.md` 13차.

### 2. ~~리더보드 26위 이하 데이터~~ → ✅ **해소 (2026-08-20, 14차)**
**Basketball Reference 리그 전체 per-game으로 대체했습니다.** top-25 한계 자체가 없어졌습니다.
`tool/fetch_bbref.py` (수집) → `tool/build_measured.py` (매칭) →
`data/stats_2025_26/measured_full.json` · `bbref/`.

- 2025-26 583명 + 2024-25 570명(결장자 폴백) · DB 174명 중 **171명 매칭**
- 폴백 3명: Haliburton·Kyrie Irving·Damian Lillard (전원 시즌 결장)
- 미매칭 **3명**: AJ Dybantsa·Cameron Boozer·Darryn Peterson (NBA 무경력 루키)
  - ⚠️ Peterson은 23차까지 **Drew Peterson**(CHO/BOS PF) 기록이 오매칭돼 있었습니다.
    폴백이 `성 + 이름 첫글자`였던 탓 → **이름 전체 접두 관계**로 강화했습니다.
- 기존 리더보드와 **149/149 일치** (매칭 정확도 교차검증)
- **13캣 중 12캣 커버.** `FGA`·`FTA`·`3PA` 확보로 **3번(시도량)도 함께 해소**
- 남은 공백은 **DD 하나** — BBRef 미집계(야후 판타지 전용 캣)

⚠️ **자격 기준 `GP >= 40`**: Kessler 5경기 OREB 4.6, A.Davis 20경기 BLK 1.7처럼
저출장 고비율을 엘리트로 올리면 안 됩니다. 자격자 164명 / 174명, 미달 10명은
`weights_data_verified: false`로 표기됩니다.

### 2a-0. ⚠️ 툴은 발행 아티팩트로 소비된다 (23차 발견)
사용자는 옥션 콘솔을 **발행된 아티팩트**로 엽니다 —
https://claude.ai/code/artifact/e75e441d-63ce-4c8c-a504-9f0ced805dca

`tool/auction-console.html`만 고치면 사용자 화면은 그대로입니다.
23차에 UI를 고친 뒤 "안 고쳐졌는데"라는 피드백을 받았고, 확인해보니
발행본이 **14차 이전 스냅샷**이었습니다(로컬은 23차 · 159KB vs 182KB).

파이프라인 마지막에 **재발행**을 붙이십시오. Artifact 도구에 위 URL을
`url` 인자로 넘기면 같은 URL이 유지됩니다. 파일은 이미 아티팩트 형식입니다
(`<title>`로 시작 · doctype/html/head/body 래퍼 없음).

### 2a-1. ⚠️ data/ 수정 전 스냅샷 필수 (사용자 지시, 2026-08-20 · 36차 갱신)
**36차부터 git 저장소입니다** — 하지만 이 규칙은 유효합니다. git이 영구 이력을 맡고
스냅샷은 **커밋 전 필드 단위 변경 요약**을 맡습니다(`git diff`는 indent=1 JSON에서
한 필드 변경에 수백 줄을 뱉어 검토가 불가능합니다 · README 「버전 관리」 절).
`data/` 를 건드리기 전에 스냅샷을 만들고 끝나면 diff 요약을 사용자에게 보여주십시오.

```bash
export SNAPSHOT_ROOT=<scratchpad>/data-snapshots
python3 tool/snapshot_data.py snap "작업명"
#  ... 작업 ...
python3 tool/snapshot_data.py diff
```

23차에 `unify_cat_weights.py` 재실행이 `cat_baselines`의 PTS·TOV 항목을 통째로 날렸는데
스냅샷이 없어서 원래 값을 **추론으로 복원**해야 했습니다. 그 사고가 이 규칙의 근거입니다.
scratchpad는 세션 전용이므로 **세션이 끝나기 전에 커밋하십시오** — 커밋하지 않으면
그 세션의 되돌리기 수단이 스냅샷과 함께 사라집니다.

### 2a. 🆕 재계산 파이프라인 순서 (23차)
`unify_cat_weights.py`가 `cat_baselines`를 다시 쓰고 `recompute_cores.py`가 그 값을 읽으므로
**순서를 지켜야 합니다**. 거꾸로 돌리면 stale 판정이 통과합니다.

```bash
python3 tool/snapshot_data.py snap "작업명"   # ← 항상 먼저 (2a-1)
python3 tool/build_measured.py      # BBRef 원본이 바뀔 때만
python3 tool/unify_cat_weights.py   # cat_weights 등급 + cat_baselines (12캣 단일 작성자)
python3 tool/recompute_cores.py     # cores.json 파생 필드
python3 tool/sync_tool.py           # 툴 임베드 상수 + P 배열
python3 tool/plant_value_reference.py   # my_max 참고선 (M5·M5b 판정 근거)
python3 tool/gen_docs03.py          # docs/03 표 생성 (26차 — 손으로 유지하면 낡는다)
python3 tool/gen_players_csv.py     # players.csv 재생성 (38차 — I25가 대조)
python3 tool/gen_docs06.py          # docs/06 전량 생성 (36차 — 그전엔 생성기가 없어 5차수 낡았다)
python3 tool/matchup_sim.py 20261020 4000   # 승률 판정 → data/matchup_sim.json (30·32차)
python3 validate.py                 # 위반 0건 확인
python3 tool/track_divergence.py    # M5·M6 진입/이탈 기준선 (27차 · 검증기는 읽기만)
python3 tool/snapshot_data.py diff  # 변경 요약 → 사용자에게 보고
git status                          # 내가 만들지 않은 변경 확인 (36차 사고)
git add <바뀐 파일들> && git commit  # 검증 통과 후에만 · **-A/-a 금지**
# → 그리고 Artifact 재발행 (2a-0 참조)
```

⚠️ **기준선 3종을 섞지 마십시오** — 23차에 이걸로 c7이 7캣 → 3캣까지 붕괴했습니다.

| 필드 | 계산 | 용도 |
|---|---|---|
| `baseline` | `cat_model.baselines()` · GP 가중 | **팀** 한계기여 (캣 승패) |
| `baseline_per_game` | `cat_model.baselines_per_game()` · 경기당·비가중 | **선수** 가중치 등급 · M1/M2 · `value_model` |
| ~~MPG≥25 풀 평균~~ | unify의 옛 자체 레시피 | **폐기** (21차에 근거 상실) |

인바리언트 19가 이 값들을 `cat_model` 계산과 직접 대조하고,
인바리언트 20이 툴 P 배열 174행의 `mx`·`mk`를 `players.json`과 대조합니다.

### 2b. DD(더블더블) 소스 🟡 → ✅ **추정으로 해소 (24차)**
BBRef는 DD를 집계하지 않습니다. 25명 리더보드가 유일한 **실측** 소스였고,
`cat_weights`의 DD 36명 중 25명만 검증됐습니다. 게임로그 수집 또는 다른 출처가 필요했습니다.

**24차 해소 근거 — 정규근사 추정 (`cat_model.dd_game_prob`)**

한 경기에서 PTS·REB·AST 중 **2개 이상이 10 이상**일 확률 × 출장경기수로 추정합니다.
각 스탯을 N(μ, σ²)로 근사하고 σ = c·√μ (c는 PTS 1.50 · REB 1.10 · AST 1.05),
임계 10에 연속성 보정(9.5)을 적용합니다. c는 **실측을 보지 않고 미리** 정했습니다 —
검증 집합에 맞춰 튜닝하면 검증이 무의미해지기 때문입니다.

실측 25명 전원 대조 결과:

| 지표 | 값 |
|---|---|
| 절대오차 중앙값 | **2.13 DD** (채택 기준 6) |
| 절대오차 평균 | 2.61 DD |
| 평균오차(부호) | **−0.65** (소폭 저추정) |
| 오차 6 이내 | 23/25명 (92%) |
| 최대 오차 | 9.6 (Moussa Diabaté) · 7.1 (Scottie Barnes) |

이로써 `evaluate()`의 `dd_target` 분기(측정 없이 +1캣)를 제거하고 DD를 다른 12캣과
동일하게 `marginal()`로 판정합니다. 7개 코어 합계는 **전부 불변**이었습니다 —
DD를 목표로 선언한 6개 코어가 실제로 DD를 이기고(+0.8 ~ +1.3), 포기한 c7이 실제로
집니다(−0.4). 가정이 우연히 7/7 다 맞았던 것이고, 이제 가정이 아닙니다.

⚠️ **남은 한계** (지우지 않는 이유):
- **추정이지 실측이 아닙니다.** 실측 25명은 2025-26 단일 시즌이고 다른 12캣은
  2시즌 혼합이라, 실측으로 덮어쓰면 기준이 섞입니다. 실측은 참고 기록으로만 둡니다.
- **독립 가정**. PTS·REB는 출장시간·사용률이 같이 밀어올려 양의 상관이 있으므로
  P(둘 다 ≥10)이 과소추정됩니다 → 빅맨 저추정 경향(평균오차 −0.65와 방향 일치).
  Diabaté(7.9P/8.7R)처럼 두 스탯이 모두 10 근처인 유형에서 오차가 가장 큽니다.
- **검증 집합 편향**. 실측 25명은 DD 상위권이라 저생산 구간은 검증되지 않았습니다.
  다만 그 구간은 추정도 실측도 0에 가까워 오차 상한이 작습니다.
- 판정 견고성은 확인했습니다: 선수당 오차 2.13 DD/시즌을 경기당으로 환산해 9명
  독립 합산하면 팀 한계기여 오차는 ±0.078이고, **가장 얇은 코어 마진(0.40)의 1/5**입니다.

### 2c. (구) 리더보드 26위 이하 🔴
현재 리더보드는 **상위 25위까지만** 확보. 그래서 22건의 하향 판정이 실측값이 아니라
"top-25 진입/미진입" 이진 판정에 근거합니다 (예: Walker Kessler `BLK` 3→1의 실제 수치는 모름).
26위 이하 데이터를 확보하면 가중치를 실측으로 재산정 가능.

### 3. ~~FTA·FGA 시도량~~ → ✅ **해소 (2026-08-20, 14차)**
BBRef per-game에 `FGA`·`FTA`·`3PA`가 있습니다. FG%·FT%도 레버리지
`(rate − 리그평균) × 시도량`으로 검증 가능해졌습니다(각 55·56명).
리그 평균은 전체 583명 시도량 가중: **3P% 35.3% · FT% 78.0% · FG% 46.7%**.

### 3b. (구) FTA·FGA 시도량 🟡
비율 캣 중 **3PT%만** 볼륨 레버리지를 계산했습니다. FT%·FG%는 시도량 데이터가 없어
정성 판단에 머물러 있습니다. FTA/FGA를 구하면 `docs/07`의 방법을 그대로 적용 가능.

### 4. 드래프트 직전 확인 (4건) 🟡
| 항목 | 이유 |
|---|---|
| 야후 실제 포지션 자격 | 데이터는 `G/F/C` 수준만. 툴에 실자격 반영해야 슬롯 배치가 유효 |
| T.J. McConnell 출장 역할 | 코어 6 A/T의 63% 담당 · 인디애나 3번째 가드 |
| 지명 시 자동 $1 입찰 여부 | 태우기 지명 전략의 전제 |
| Sabonis·Haliburton 프리시즌 상태 | 코어 5는 Sabonis 건강 확인이 발동 조건 |


### 5. 소속 미확인 2명 🟢
Svi Mykhailiuk · Russell Westbrook (`team: "—"`). 확인되면 갱신.
Duncan Robinson도 `소속 재확인 권장` 플래그.

### 6. 검증 못 한 주장들 🟡
- 코어 4의 "PTS·TOV 포기가 나머지 7캣 승리로 이어지는가"
- 코어 3의 "SGA FT%가 빅맨 FT% 붕괴를 상쇄"
- 과열 피벗의 "OREB·BLK 포기 후 가드·윙 전환이 7캣 승리로 이어지는가"

전부 **주간 팀 총량 모델**이 필요하고, 그건 2번(26위 이하 데이터) 없이는 만들 수 없습니다.

---

## 데이터 모델 · 불변식

### `data/players.json` (174개)
```
name team pos market_low market_high my_max surplus obtainable injury_exclude
tag(buy|burn|dart|null) cats cat_weights
measured_2025_26  team_change  volume_leverage  at_marginal_lift  verdict  flag
my_max_basis  tag_basis  tag_basis_auto      ← M5·M5b·M6의 근거 필드 (26~30차)
value_reference{z_total,z_by_cat,dollar_naive,rank_by_value,rank_by_my_max,
                rank_divergence,gp_adjusted_my_max}   ← plant_value_reference.py 생성
```
- `my_max_basis`/`tag_basis`의 `auto` 항목은 생성 시 `conditions`(그때의 `core_hits`·
  `obtainable`)를 함께 적고, 조건이 바뀌면 **근거가 자동 무효화**돼 다시 위반으로 뜹니다
  (`tool/close_unused_ceilings.py` · `divergence_rules.has_any_basis`)
- **노출 = `max(dollar_naive, market_high)`** — my_max를 포함하지 않습니다(자기참조 방지).
  M6의 정렬 전용이고 **절대액은 신뢰하지 않습니다**(z 합산이 상단을 압축)
- `surplus = my_max − round((market_low+market_high)/2)` — 플러스면 승부처
- `obtainable = my_max ≥ market_low` — false면 낙찰 자체가 불가 (35명)
- `injury_exclude` — 코어·대체후보·피벗에서 전면 제외 (현재 Jimmy Butler III 1명)
- `cats` 표기: `OREB3 BLK2` = OREB 엘리트(3)·BLK 플러스(2). `TOV`는 양수 = 턴오버 적음

### `data/cores.json`
```
decision_oneliner        한 줄 판단 요약 (README·툴이 같은 문장 참조)
decision_table[]         조건부 우선순위. cond.type = hot_bigs|prices|default_normal|anchor_fail|manual
overheat_thresholds[]    센터 과열 임계값 단일 소스. 전부 "> $X" 형식
injury_exclude_rule      장기 부상 제외 규칙 서술
overheat_tiers           과열 계층 3종 정의(name_big / low_cost_center / anchor)
anchor_policy            앵커 정책 정의 + on_fail 3종 규칙
opponent_baseline        상대 슬롯당 실측 기준선 (PTS 18.68 · AST 4.42 · TOV 2.19)
matchup_sim_ref          → data/matchup_sim.json (32차 분리 · 여기에 시뮬 결과를 넣지 말 것)
kat_single_point         KAT 제외 maximin 재탐색 결과 (33차)
c7_old                   구 c7 보존 (33차 전면 교체 전 원본)
banchero_removal_note    죽은 대체후보 제거 위치 (28차)
cores[]
  ├ slots[]              slot role is_anchor plan_price candidates[{name,plan_price}]
  │                      + bid_ceiling / expected_cost (35차 · plan_price는 후자의 별칭)
  ├ big_budget_cap/planned, c_eligible_count, budget_slack(예비비)
  └ pivot_plan
      ├ triggers[] swaps[] final_roster[] final_total/big_budget/c_eligible
      └ fallback           condition_rules[] condition_logic swaps[] final_roster[]
```

### 반드시 지켜야 하는 불변식 (`validate.py`가 검사)
1. 모든 후보: `market_low ≤ plan_price ≤ my_max`
2. 9슬롯 완성 `PG SG SF PF C UTIL×2 BN×2`
3. `PG/SG→G` · `SF/PF→F` · `C→C` 자격 (1순위 **및 대체 후보 전부**)
4. 총액 ≤ $200 (및 ≥ $180)
5. 빅맨 예산 ≤ `big_budget_cap`
6. 비앵커 슬롯은 대체 후보 2명 이상 (앵커는 면제 — 실패 시 코어 전환)
7. `injury_exclude` 선수가 어디에도 없음
8. 피벗·백업의 노리는 캣/포기 캣 명시
9. 트리거·백업 조건이 `overheat_thresholds`와 일치
10. 판단표가 7개 코어 전부 포함 · 우선 0 = 코어 7 · 임계값 ≤ `my_max`
11. **과열 임계값 2계층** — `hot_bigs`가 `counts_toward_core7=true` 계층만 참조 ·
    선수 목록이 그 계층과 정확히 일치 · `signal="overheat_at"` ·
    `overheat_at > expected_2026_27` · 철수가 ≥ 최대 계획가 · `anchor`는 `overheat_at=null`
12. **툴 임베드 동기화** — 툴 HTML의 `DECISION`·`DECISION_ONELINER`·`OVERHEAT`·`OTIERS`·
    **`CORES`·`PIVOTS`(6종)** 가 `cores.json`과 일치 · `hotCenterCount`가 `overheated()` 사용
13. **앵커 정책** — 모든 앵커에 `anchor_plan` · `effective_headroom` 실계산 일치 ·
    `on_fail` 3종(`substitute` 이중세계 유효 대체 필수 / `switch_core` 목적지가 같은 앵커 비의존 /
    `pivot` 피벗 로스터에서 실제 제외) · 재적합불가+대체0 → `conditional_on_discount` 필수 ·
    치환 총액 <$180이면 `redeploy` 명세 필수
14. **이중 보관 값 = 실계산 일치** — `slots[].plan_price` == `candidates[0].plan_price` ·
    `planned_total`·`big_budget_planned`·`c_eligible_count`·`budget_slack` 전부 실계산 대조
15. **툴 `P` 배열 동기화** — 툴 HTML `const P=[...]`의 선수별 `mk`(시장가)·`c`(cats)가
    `players.json`과 일치
18. **캣 선언 = 실측 팀 한계기여** (18차 통합) — `cat_baselines` 12캣 단일 소스 ·
    M1 단조성·M2 한계기여≤0 금지를 **전 캣**에 적용 · `use_marginal_lift_field`(A/T는
    원 비율 금지) · **코어의 targeted/punted 선언이 선발 7명 실측 한계기여와 일치** ·
    `cat_team_marginals` 기록값이 실계산과 일치.
    ⚠️ 가중치 합(≥6) 규칙은 음수 기여를 표현 못 하므로 **판정 기준으로 쓰지 말 것**
18. (구) PTS 팀 한계기여 — `opponent_baseline.per_slot_PTS` 필수 · `pts_team_marginal`이
    실계산과 일치 · **PTS를 목표로 선언한 코어/피벗/백업은 팀 한계기여 > 0** ·
    피벗·백업 `cat_marginals`(PTS·TOV) 일치. PTS는 많을수록 좋으므로 한계기여 = 실측 − 기준선
17. **TOV 팀 한계기여** — `opponent_baseline` 필수 · `tov_team_marginal`의 선발 집계·
    한계기여·verdict가 실계산과 일치 · **TOV를 목표로 선언한 코어는 팀 한계기여 > 0**.
    ⚠️ 가중치 합(≥6) 규칙으로 TOV를 판정하지 말 것 — 음수 기여를 표현할 수 없습니다.
16. **`cat_weights` 규칙 (14~15차, 전체 스탯 기반)** — M1 자격자(GP≥40) 안에서 가중치는
    실측값의 단조함수(볼륨 캣 원값 · 비율 캣 레버리지 · TOV 역순) · M2 A/T 한계기여가
    음수면 가중치 금지 · M3 GP<40은 `weights_data_verified: false` ·
    **M4 `flag`/`verdict`가 가중치와 정면 충돌 금지**(폐기된 "top-25 밖" 근거가
    서술문에만 남는 것을 차단. 정정문의 인용은 예외).
    ⚠️ 비율 캣은 **반드시 시도량으로 레버리지 계산**. 원값 비교하면 저볼륨 고효율 선수를
    잘못 잡습니다(패턴 ③) — 13차에 저자가 실제로 이 실수를 했습니다.

19. **`cat_baselines` = `cat_model` 실계산** (I19) — 13캣 전부 존재 · 척도 일치 ·
    A/T는 `use_marginal_lift_field`(원 비율 금지) · TOV는 `lower_is_better` 필수
20. **툴 `P` 배열 대조** (I20) — 174행의 `mx`(my_max)·`mk`(시장가)가 `players.json`과 일치
21. **계획가 > 시장 상단** (I21 · **경고 등급**, `err` 미가산 · 31차) — 35차에 0건이 됐지만
    검사는 남겨 재발을 상시 노출합니다
22. **예비비** (I22 · 34차) — `$200 − 계획총액` · 목표 ≥$12 · 경고 <$8 · **위반 <$4** ·
    **>$25는 "과소 편성" 경고**. 근거: 앵커 1명이 시장 상단으로 가면 예산이 넘습니다
23. **가격 3필드 정합** (I23 · 35차 · 361 엔트리) — `bid_ceiling ≤ my_max` ·
    `expected_cost ≤ bid_ceiling` · `plan_price == expected_cost` · `expected_cost ≤ 시장 상단`
24. **피벗 로스터 = base 1순위 + swaps** (I24 · 37차) — stale swap 차단
25. **`players.csv` = 생성기 출력** (I25 · 38차) — `tool/gen_players_csv.py`의 `build()`를
    직접 불러 대조합니다(규칙 이중 구현 금지 · 27차 사고와 같은 이유).
    ⚠️ 도입 시점에 **174행 전부 불일치**였습니다 — `cats`는 13차 재산정 미반영,
    스탯은 20차 2시즌 혼합 미반영, `*_lift`는 옛 기준선. 손으로 유지되는 미러였습니다.
    `players.json`을 고치면 **재생성해야 하고, 안 하면 exit 1**입니다

**M1~M6** (`tool/divergence_rules.py` 단일 소스 · 선수 평가층):
M1 단조성 · M2 한계기여 ≤0인데 가중치 부여 금지 · M3 `GP<40`은 `weights_data_verified:false` ·
M4 서술문(`flag`/`verdict`)↔가중치 정면 충돌 금지 · **M5** tag 괴리에 근거 필수 ·
**M5b** 근거가 인용한 `div`의 드리프트 검사(±2) ·
**M6** tag 무관 괴리(`|div| ≥ 40` **또는** `노출 ≥ $25 이면서 |div| ≥ 20` · 28차).
`|div| 18~22`는 **경계 감시**로 출력만 하고 `err`에 가산하지 않습니다.

**코어를 수정하면 `validate.py`를 반드시 통과시킬 것.** 위반 시 exit 1.

---

## 작업 시 함정 (경험한 것)

### 문자열 치환은 조용히 실패한다 — 이 프로젝트에서 4번 발생
| 사례 | 원인 |
|---|---|
| SGA 수치 정정 2회 실패 | 치환 대상 문자열이 이전 감사에서 이미 변경됨 |
| 코어 7 삽입 실패 | `re.sub` 정규식 미매치. `{id:"c\d"` 카운트 7을 통과로 착각(c0 포함) |
| OVERHEAT 치환 실패 | `json.dumps` 출력의 공백 때문에 패턴 불일치 |
| `\b88명\b` 치환 실패 | 한글이 `\w`에 포함되어 `명`↔`이` 사이에 word boundary 없음 |

**대응 3종을 함께 쓸 것**
1. 치환보다 **구조 단위 재작성** (엔트리 전체를 정규식으로 잡아 통째로 교체)
2. 치환 후 **반드시 파싱해서 존재 확인** (print만 찍고 넘어가지 말 것)
3. **assert 조건 자체를 먼저 검증** — 한 번은 assert가 틀려서 스크립트가 죽고
   파일이 저장되지 않은 채 "수정됐다"고 보고한 적 있음

### 정적으로 적어둔 안내는 반드시 낡는다
정적 → 데이터 생성으로 바꾼 이력: 피벗(3차) · 과열 임계값(4차) · 백업 조건(6차) ·
판단표(7차) · 전환 트리거 패널(8차) · **툴 DECISION 상수(10차)**.
**새 안내를 추가할 때 정적 HTML/문장으로 쓰지 말 것.**

### 같은 값을 두 곳에 두면 반드시 갈라진다 — 11차에서 2건 더 · 38차에 2건 더

38차에 같은 형태를 두 건 더 찾았습니다.
- **`players.csv`** — README가 "같은 데이터 표 형식"이라 적었지만 생성기가 없는 손 미러였고
  **174행 전부** 갈라져 있었습니다(`tool/gen_players_csv.py` 신설 · I25가 검사)
- **주간 경기수 3.5** — `matchup_sim`·`gen_docs03`·`rate_cat_leverage` 세 파일에 각각
  하드코딩. `cat_model.GAMES_PER_WEEK`로 모았습니다(값도 3.299로 정정)

**"생성물이라고 문서에 적어놓고 생성기가 없는 것"이 이 저장소의 반복 패턴입니다** —
`docs/06`(37차) · `players.csv`(38차)가 연달아 같은 형태였습니다.
- `slots[].plan_price` vs `candidates[0].plan_price` — 검증기는 전자를 쓰는데 후자만
  고쳐서 총액이 조용히 옛 값으로 계산됐습니다(8건)
- `planned_total`·`big_budget_planned`·`c_eligible_count` — 파생 합계인데 따로 저장돼 낡음(4코어)

**이제 이중 보관된 값은 전부 검증기가 실계산과 대조합니다.** 계획가를 하나 고치면
슬롯·후보·파생합계·툴 상수를 **모두** 갱신해야 하고, 안 하면 `exit 1`입니다.

✅ **이 트랩은 이제 검증기가 막습니다** (10차). `validate.py`가 툴 HTML의 임베드 상수를
`cores.json`과 직접 비교합니다 — `DECISION` · `DECISION_ONELINER` · `OVERHEAT` · `OTIERS`,
그리고 `hotCenterCount`가 `overheated()`를 쓰는지까지. **cores.json을 고치면 툴 상수도
재생성해야 하고, 안 하면 exit 1입니다.**

### 문서는 데이터에서 생성한다 — 🔴 "생성된다"고 적어놓고 생성기가 없었다 (36차)
`docs/03`은 `tool/gen_docs03.py`(표만 · 산문 수기), `docs/06`은 `tool/gen_docs06.py`(전량)가
생성합니다. 손으로 고치면 다음 재생성에 날아가고, 더 나쁘게는 데이터와 갈라집니다.

⚠️ **36차 이전에는 `gen_docs06.py`가 없었습니다.** README와 이 문서 둘 다
"docs/06은 cores.json에서 전체 재생성된다"고 적어놨는데 생성기가 존재하지 않았고,
그래서 아무도 갱신하지 않은 채 **5차수(33~35차)를 낡았습니다**:

| 항목 | 낡은 docs/06 | 실제 |
|---|---|---|
| c7 | '반센터 인플레' · 빅맨 $25/$36 | **중가 센터 전환**(A1) · $107/$130 |
| 코어 총액 | c1 $187 · c2 $199 · c6 $185 | $182 / $187 / $181 (7개 중 6개 불일치) |
| McConnell | 21회 등장 | 1순위로 쓰는 코어 **0개** |
| 예비비·승률·maximin | 0회 | 34·30차 도입 개념이 통째로 없음 |
| 플랜 수 | "15개(백업 1 포함)" | **14개** — 백업은 33차 c7 교체로 소멸 |

**교훈: "자동 생성됨"이라는 표기 자체가 검사 대상입니다.** 그 표기가 붙은 문서는
사람이 안 읽고 안 고치므로, 생성기가 없으면 가장 조용히 썩습니다.
새 문서에 그 표기를 붙이려면 **생성기를 먼저 만들고 파이프라인에 넣으십시오.**

### 탐색 결과는 승자의 저주다 (34차)
900시행 maximin 1위를 채택했다가 **6000시행·같은 난수 스트림 재측정에서 뒤집혔습니다.**

| 사례 | 900시행 | 6000시행 |
|---|---|---|
| c6 `Şengün → LeBron` | 38.9% (1위) | 35.1% (3위) — **폐기** |
| c4 `Randle → D.White` | 28.0% (1위) | 26.7% — 탈락 |
| c4 `McConnell → Trae Young` | 26.6% | **30.1%** — 채택 |

**1~2%p 차이는 노이즈입니다.** 탐색으로 상위 후보를 좁히고, **채택 전에 반드시 같은 스트림
고시행으로 재대조할 것.** c6는 이 절차로 자기 정정했고(Edgecombe안이 LeBron안을 전 축에서
지배 · 예비비 $2 → $19), c4는 순위가 실제로 뒤집혔습니다.

### 승률표를 읽을 때 실행 가능성을 먼저 볼 것 (34차)
33차의 "우선 0을 c6로" 권고가 **철회**된 이유입니다 — 76.8%는 그 시나리오에서
**조립할 수 없는 로스터**의 승률이었습니다. 조건부 코어의 승률은 반드시
**그 조건이 참인 세계의 가격으로 강제 매수**시킨 뒤 총액을 확인하고 읽으십시오.

### "빠뜨려도 조용히 통과한다" — 이 프로젝트의 대표 실패 형태 (30차에 6번째)
툴 임베드 상수(7회) · `plant.py` 라벨 퇴행 · 규칙 이중 구현 ·
`has_any_basis(hits_fn=None)`이 auto basis 27건을 **영구 유효**로 만든 것 ·
`_core_hits` 3중 구현 · I23이 `e["name"]`만 읽어(**슬롯 dict엔 `name`이 없다**)
슬롯 레벨 검사가 통째로 조용히 통과한 것.

→ **새 검사를 넣으면 반드시 음성 테스트**(일부러 위반을 주입해 잡히는지 확인)를 돌릴 것.
→ **기본값으로 검사를 끌 수 있게 만들지 말 것.** 30차는 `hits_fn` 누락 시
   `MissingHitsFn` 예외를 던지도록 고쳤습니다.

### 시뮬 산출물을 계획 파일에 쓰지 말 것 (30·32차)
`matchup_sim`이 **상대 로스터 이름**을 `cores.json`에 기록하자 `core_hits`가 그것을
"우리 플랜 등장"으로 세어 auto basis 6건이 무효화되고 M6 위반 6건이 떴습니다.
자동 무효화는 정상 작동했고 **신호가 오염**된 것입니다.
→ `data/matchup_sim.json`으로 분리하고 `cores.json`엔 `matchup_sim_ref`만 둡니다.

### 조립 알고리즘은 예산을 남기고 진동한다 (30차)
상대 로스터 자동 조립에서 ① score/dollar만 보고 $200 중 **$61**만 씀 ② 최하위 교체 규칙 탓에
Josh Hart ↔ Ausar Thompson 무한 진동 ③ 실패 시 `None`을 돌려줬는데 호출부가 "무작위 상대"로
해석해 **빅스택 열이 평균 상대의 복제본**이 됨. → 업그레이드 패스 추가 · `FAILED` 센티넬로 분리.

### 비율 캣의 상대 마진을 계수 캣과 같은 분모로 나누지 말 것 (29차)
`rel_margin`이 비율캣을 `b×9`로 나눠 **FG%를 10.5배** 부풀렸습니다(캣마다 배율이 다름 —
팀 시도량이 다르니까). 옛 공식은 네 후보를 전부 "3%↑ 7캣"으로 뭉개 **판별력을 죽였습니다.**
정정 공식은 `(rate − b)/b`이고, 그마저 **분모가 σ가 아니라 기준선**이라 캣 간 비교는
여전히 불가입니다 → 그래서 30차의 승률 모델이 필요했습니다.

### 음성 결과를 주장할 때는 탐색 깊이를 먼저 의심하라 (39차)

34차 「탐색 결과는 승자의 저주」의 **거울상**이다. 그건 얕은 탐색이 **노이즈를 1위로**
올리는 문제였고, 이건 얕은 탐색이 **존재하는 답을 놓쳐 음성 결론을 만드는** 문제다.

39차 c2 재설계에서 같은 제약·같은 목적함수로:

| 탐색 규모 | 제약 통과 조합 | 등반 국소최적 | 최선 평균 | 최선 최저 |
|---|---|---|---|---|
| 4,000시행 | 138 | — | 78.1% | 60.8% |
| 30,000시행 + 등반 | 2,112 | 1,056 | **84.6%** | 65.9% |
| 120,000시행 + 등반 | 8,248 | 4,124 | 84.0% | 66.1% |

**+6.5%p가 탐색 깊이만으로 왔다**(78.1 → 84.6). 그리고 규모를 **4배 더 늘렸을 때
−0.6%p**로 옆으로 움직였다 — 두 번째가 진짜 개선이고 **세 번째가 수렴 증거**다.
84~85%가 이 제약의 천장이다.

→ **음성 결론은 규모를 늘려도 안 바뀌는 것을 보인 뒤에만 낼 수 있다.** 1차 결과로 "겹침 제약 하에서는 현행보다 나은 조합이
없다"고 보고했고, 평가 세션은 그것을 받아 **c2 폐기로 기울었다.** 둘 다 틀렸다.

**후자가 더 위험하다** — 승자의 저주는 재대조하면 잡히지만, **놓친 답은 아무도 찾으러
가지 않는다.** 음성 결론은 그 자체로 탐색을 멈추게 만든다.

→ **"제약 하에서는 답이 없다"는 결론은 탐색이 수렴했다는 증거 없이는 낼 수 없다.**
   최소한 ① 규모를 한 자릿수 늘려 재실행하고 ② 국소 최적 등반을 붙이고
   ③ 두 결과의 최선값을 비교해 **더 이상 안 올라가는지** 보여야 한다.
   "못 찾았다"와 "없다"는 다른 주장이다. 재현: `C2_TRIES=120000 python3 tool/c2_search.py`

### 퍼센트처럼 보이는 원값 — `weight`는 비율이 아니다 (39차)

`players.json`의 `measured_source.seasons[].weight`는 **0~1 비율이 아니라 GP 기반 원 가중치**다
(`w25 = GP25 × 1.5` · `w24 = GP24`).

```
Trae Young    w25 22.5 (GP 15)   w24 76.0 (GP 76)   합  98.5
Sabonis       w25 28.5 (GP 19)   w24 70.0 (GP 70)   합  98.5
Nikola Jokić  w25 97.5 (GP 65)   w24 70.0 (GP 70)   합 167.5   ← 여기선 안 속는다
```

🔴 **합이 100 근처가 되는 것은 저출장 선수에서만**이고, **하필 그들이 감사 대상**이다.
`22.5`를 그대로 "23%"로 읽으면 **정답이 나온다**(실제 비중 22.8%). 틀렸다는 신호가 없다.

39차에 GP 출처를 감사하면서 `weight < 0.15`(비율로 가정)로 걸렀다가 **완전 투영 3명만 잡고
Trae(22.8%)·Sabonis(28.9%)를 통째로 놓쳤다.**

```python
실측_비중 = w(2025-26) / (w(2025-26) + w(2024-25))
```

⚠️ **그리고 정규화된 값이 이미 데이터에 있었다** — `measured_source.blend_share_2025_26`이
정확히 그 값이다. 없는 걸 만든 게 아니라 **있는 걸 안 쓰고 원값을 손으로 해석한 사고**다.
정규화가 필요하면 `tool/gp_provenance.py`의 `measured_share()`를 쓸 것.

**필드 이름에 단위가 없으면 원값을 의심할 것** — 이 저장소에는 `weight`·`share`·`ratio`가
섞여 있고, `share`만 비율이다.

### `git commit -a`/`add -A`를 쓰지 말 것 (36차에 실제로 사고)

git 전환 당일, 두 번째 커밋을 `git commit -a`로 만들었더니 **동시 진행 중이던 다른 세션의
작업**(`docs/06-cores.md` 688줄 재생성 · `docs/02`)이 함께 들어갔습니다. 커밋 메시지는
제 파일 3개만 나열하고 "데이터·판정 무변경"이라고 적혀 있었으므로 **이력이 실제 내용과
갈라졌습니다** — 이 프로젝트가 가장 경계하는 실패를 커밋 로그에서 재현한 셈입니다.

여러 세션·에이전트가 같은 디렉터리를 동시에 만질 수 있습니다. **커밋 전에 `git status`로
내가 만들지 않은 변경이 있는지 보고, 파일을 명시해서 add 하십시오.**
복구는 `reset --mixed`(작업트리 보존) → 커밋 분리 → `--force-with-lease`이고,
전후 `shasum -c`로 유실 0건을 확인해야 합니다.

### 툴 수정 시
- **모달 API 금지** — Artifact는 샌드박스 iframe이라 `confirm/alert/prompt`가 차단됨
  (초기에 `confirm()` 때문에 초기화 버튼이 작동하지 않았음)
- **외부 URL 금지** — 오프라인 자립 유지 (현재 모달 0 · 외부 URL 0)
- 위임 클릭 핸들러의 `closest()` 셀렉터에 **새 `data-*` 속성 추가를 잊지 말 것**
  (`data-jump` 누락으로 코어 후보 버튼이 죽은 적 있음)
- `localStorage` 키를 올리면 사용자 상태가 초기화됨 (현재 `auction13cat_v9`)

---

## 평가 이력

감사를 **35차례** 진행했습니다(외부 에이전트 평가 8차례 포함). 전부 `docs/04-audit-log.md`에 기록.
사용자가 직접 잡아낸 오류도 5건(Dort·Sheppard·Kennard·Camara·Trent Jr.) 있고,
그중 저자가 사전에 잡은 것은 **0건**입니다.

**9차 감사(2026-08-20)**: 사용자 지적 "Dejounte Murray는 뉴올 주전 PG인데 낮게 평가했네?"
→ 가격($8)은 방향이 맞았으나 **credited 캣 3개 중 2개(PTS2·STL2)가 실측 근거 0**.
패턴 ①이 `cat_weights` 층에 남아 있음을 발견. 사용자 발견 6건째, 저자 사전 발견 여전히 0건.

**13차(2026-08-20)**: 그 지적을 따라 `cat_weights`를 전수 검증해 **26건 수정**.
동시에 **패턴 ③(rate만 보고 볼륨 무시)을 저자가 스스로 재발**시켰습니다 —
FT%를 단조성 규칙에 넣어 저볼륨 고효율 선수(Cam Spencer 94.0%)를 기준으로 12건을
위반 판정했다가 되돌렸습니다. **비율 캣은 시도량 없이는 어떤 비교도 하면 안 됩니다.**

**오류 패턴 3종** (재발 주의)
1. **평판을 캣 기여로 착각** — 계약 규모·수비상·이름값을 실측 없이 엘리트 가중치로 (22건)
2. **출처 한 줄에 근거한 가격** — 남의 밸류를 재계산 없이 채택 (Reed Sheppard)
3. **rate만 보고 볼륨 무시** — 비율 캣은 시도량 지분만큼만 레버리지 (17건 + A/T 재발)

---

## 드래프트 당일 런북

1. 툴 열고 `판단 순서` 카드 확인 → 기본은 **코어 6**
2. 초반 엘리트 빅맨 경매 **불참**. 지명권이 오면 태우기 지명
   (`Luka` → `Ant Edwards` → `Giannis` → `JJJ` → `Myles Turner`)
3. **타팀 낙찰가를 계속 기록** → 3명 넘으면 보정 계수 표시 → `보정 적용` 켜기
4. `센터 과열 감지`에 2명 이상 뜨면 **즉시 코어 7** (피벗보다 먼저)
5. 상시 감시 지표: 상단 **`입찰 한도`**(= 잔액 − 남은 슬롯 + 1)와 **`슬롯당 예산`**($8 미만 위험)
6. 10초 룰이므로 **판단하지 말고 최대가만 볼 것.** 최대가 초과 입찰 금지
