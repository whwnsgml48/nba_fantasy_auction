# 트리거 발동 가능성 · `my_max` 상단 압축 (39차 · B)

평가 세션 지시 B-1·B-2에 대한 답입니다. **데이터는 한 줄도 바꾸지 않았습니다** —
진단과 권고만 합니다. 재현: `python3 tool/trigger_audit.py`

---

# B-1 · 판단표 트리거가 실제로 발동할 수 있는가

## 결론 먼저 — 평가자의 핵심 주장은 **모델 가정의 산물**입니다

지시서는 "7행 중 3행이 무의미, c2·c3는 도달 불가능한 죽은 분기"라고 했습니다.
그 계산은 **낙찰가가 `[market_low, market_high]` 안에 균등하다**는 가정을 씁니다.
그 가정이 틀렸다는 것을 **같은 저장소의 데이터가 직접 반증합니다.**

`market_low/high` 는 **추정 구간이지 지지집합(support)이 아닙니다.** 구간 밖 확률을
0으로 못 박으면 "우리 추정이 빗나갈 가능성"이 정의상 0이 됩니다. 실제로는:

| 조건 | 균등 P | 같은 선수의 **작년 실낙찰가**(스케일 후) | 균등 모델의 판정 |
|---|---|---|---|
| `Evan Mobley > $30` (시장 $20-26) | **0.00** | **$50** — 실제로 넘었다 | "절대 안 일어남" ❌ |
| `Ivica Zubac > $16` (시장 $8-14) | **0.00** | **$41** — 실제로 넘었다 | "절대 안 일어남" ❌ |
| `Alperen Şengün > $34` (시장 $23-29) | **0.00** | **$70** — 실제로 넘었다 | "절대 안 일어남" ❌ |
| `Mark Williams > $14` (시장 $4-10) | **0.00** | **$15** — 실제로 넘었다 | "절대 안 일어남" ❌ |

균등 모델이 "절대 불가"로 찍은 18건 중 **4건은 작년에 실제로 일어난 사건**입니다.
이 모델로 코어를 삭제하면 안 됩니다.

## 대신 쓴 것 — 작년 실측에서 뽑은 **잔차비** 분포

지시대로 `data/prior_auction_2025_26/results.json`(120건)을 썼습니다. 다만
`실낙찰가 ÷ 현재 추정 중간값`을 그대로 쓰면 **순환**입니다 — 현재 곡선이 그 실측을
**순위 보존 재적합**한 값이라(`proposed_market_refit.json.method`) 선수 개별 편차가
구성상 이미 지워져 있습니다. 그래서 작년 데이터 **안에서** 잔차를 만들었습니다:

```
1. 작년 지명 120명 중 우리 DB·z순위 보유 92명 = S
2. S의 실낙찰가 내림차순 = 가격 사다리
3. 각 선수의 z-score 모델 순위(가격과 독립)로 사다리에서 예측가를 읽는다
4. 잔차비 = 실낙찰가 ÷ 예측가        "우리 순위가 맞았다면 받았을 가격 대비 실제"
```

✅ **스케일 문제는 구성상 소거됩니다.** 분자·분모가 둘 다 작년 달러라 12팀→14팀
스케일이 비율에 들어오지 않습니다. 지시서가 "확인하라"고 한 조건입니다 —
잔차비 중앙값 **0.89**(1.0에서 0.11)로 재확인했습니다.

### 🔴 풀링하면 안 됩니다 — 산포가 순위에 따라 완전히 다릅니다

| 우리 z순위 | n | 중앙값 | 사분위 |
|---|---|---|---|
| 1~10 | 10 | **0.50** | 0.26~0.86 |
| 11~25 | 15 | 0.76 | 0.60~0.97 |
| 26~45 | 20 | 0.92 | 0.55~1.39 |
| 46~70 | 25 | 0.83 | 0.46~1.45 |
| 71~ | 22 | **3.75** | 1.00~5.75 |

우리가 상위로 본 선수를 시장은 **절반값**에 팔고, 우리가 하위로 본 선수를 시장은
**3.75배**에 삽니다. 전체 풀링(범위 0.07~17.33)을 쓰면 상위권 조건의 확률이 하위권의
거대한 상방 꼬리로 오염됩니다. 그래서 **순위 국소 창**(±20)에서만 잔차비를 뽑습니다.

> 이 표 자체가 B-2(상단 압축)의 독립 증거입니다. 같은 현상을 반대편에서 본 것입니다.

### 🔴 그러나 잔차비는 `docs/08` §0 경고를 피하지 못합니다 (39차 평가 세션 지적 · 수용)

> **`docs/08` §0**: *"작년 낙찰가는 **2025-26 프리시즌 기대치**이고 `my_max`는
> **2025-26 실측 성적**이다. 두 값을 선수별로 직접 비교하면 '우리 추정이 틀렸다'가 아니라
> **'기대 vs 결과'의 차이**를 재게 된다."*

사다리로 분모를 바꿔 **순환은 끊었지만 이 오염은 그대로입니다.** 잔차의 상당 부분이
가격 산포가 아니라 **시즌 중 성적 변화**입니다 — Jamal Murray(0.26)·Barnes·Maxey처럼
작년 프리시즌에 싸게 팔리고 시즌 중 올라온 선수들이고, **올해 드래프트에서 재현되지
않습니다.** 순위 일치도(`|z순위 − 시장순위| ≤ 10`)로 조건화해도 사라지지 않습니다 —
그 집단에 브레이크아웃이 그대로 들어 있기 때문입니다.

**그래서 세 번째 모델이 필요합니다. 셋을 통합하지 않고 나란히 냅니다.**

### ③ 엘리트 대조군 — 오염이 없는 부분집합

작년에 **이미 비쌌던** 선수($60+ 스케일 후 · **n = 11**)로 좁힙니다. 시장이 이미 알고
있던 선수라 브레이크아웃 아티팩트가 없습니다. 비율 = `작년 실낙찰가(스케일) ÷ 올해 추정 중간값`:

```
0.81 Cade · 0.91 Edwards · 0.97 SGA · 0.99 Luka · 0.99 Jokić · 1.01 Wemby
1.31 Giannis · 1.64 KAT · 1.90 Trae · 2.58 Şengün · 3.59 Sabonis
```

**분포가 한쪽으로만 열려 있습니다.** 진짜 엘리트는 0.81~1.01에 촘촘히 모이고 위쪽 꼬리는
전부 **하락한 선수**입니다. 즉 **엘리트는 기대보다 싸지는 일이 거의 없습니다.**

### 🔴 이 모델을 **개별 선수 가격 판정에 쓰면 안 됩니다** (39차 · 제가 한 번 틀렸습니다)

저는 이 표를 근거로 *"KAT은 작년 지출 6위/120인데 우리는 18위/174 · $45로 본다 —
재검토가 필요하다"* 는 주장을 **유효하다고 지지했습니다. 틀렸습니다.**
평가 세션이 외부 9캣 컨센서스로 계층 11명을 대조해 반증했습니다:

| | 외부 순위 | 우리 시장순위 | Δ | 작년 배율 |
|---|---|---|---|---|
| Wembanyama | 1 | 2 | +1 | 1.01 |
| Jokić | 2 | 1 | −1 | 0.99 |
| **KAT** | **19** | **18** | **−1** | **1.64** |
| Şengün | 35 | 37 | +2 | 2.58 |
| Sabonis | 46 | 50 | +4 | 3.59 |

**11명 전원 Δ ±5 안**입니다. 우리 시장 추정 순위는 이 계층에서 어긋나지 않습니다.
계층 중앙값 1.64의 정체는 "우리가 이 계층을 싸게 본다"가 아니라
**"이 계층에 올해 하락자가 많았다"** 이고, 외부 컨센서스도 그 하락에 동의합니다 —
어긋나는 것은 **작년 시장뿐**입니다.

> **경계선**: 작년 실낙찰가는 **계층 판정**에는 쓸 수 있지만(`docs/08` ③의 센터 이봉분포)
> **개별 선수 가격 판정**에는 쓸 수 없습니다. 그 구분선은 **외부 컨센서스**입니다 —
> 우리 순위가 외부와 맞으면 작년과의 괴리는 우리 오류가 아니라 작년 시장의 움직임입니다.

이것은 위에서 제가 잔차비에 대해 인정한 `docs/08` §0 오염('기대 vs 결과')과 **같은 함정**이고,
경고를 적어놓고도 다른 통로로 다시 밟았습니다.

⚠️ **KAT에 대한 추가 주의** — 그의 `my_max $71` vs 시장 중간 $45(비율 **1.58**)는
**압축이 아니라 의도된 아비트라지**입니다. 9캣 시장이 그를 #19로 보는데 13캣(DD 56 리그 1위 ·
OREB 3.1)에서는 그보다 높다는 것이 우리 전제이고, 외부 컨센서스가 #19라는 사실은
**우리 시장 추정이 맞다는 확인**이지 우리 평가가 낮다는 뜻이 아닙니다.
엘리트 대조군의 배율 1.64는 **시장 대 시장**(작년 시장 vs 올해 우리 시장 추정) 비교이지
**우리 평가와는 무관**합니다. 두 층을 섞어 읽지 마십시오.

⚠️ 이 모델의 고유 한계 3종 — **숫자만 떼어 쓰지 마십시오**:
- **n = 11.** 확정 주장이 불가능한 크기입니다. 한 명이 들고 나면 P가 0.09씩 움직입니다.
- **위쪽 꼬리는 시장 노이즈가 아니라 우리 자신의 재평가입니다.** Sabonis 3.59는
  "작년 $68짜리를 올해 $19로 본다"는 뜻이고, 그 재평가가 맞으면 실제가는 우리 추정 쪽에
  붙습니다. 추정 오차 표본으로 쓰면 **상방으로 보수적**이 됩니다.
- **엘리트에만 정의됩니다.** 저가 선수에는 대조군이 없습니다.

## 발동 확률표 (세 모델)

`python3 tool/trigger_audit.py` 전체 출력. 판단표 부분만 옮기면:

| 우선 | 조건 | ① 균등 | ② 국소 잔차비 | ③ 엘리트 대조군 | 같은 선수 작년 |
|---|---|---|---|---|---|
| 1 · c1 | KAT ≤ $50 **AND** Hali ≤ $56 | 0.17 | 0.52 | **0.10** | KAT $74 ✗ |
| 3 · c3 | SGA ≤ $72 | **0.00** | 0.59 | **0.09** | $83 ✗ |
| 조건부 · c2 | Jokić ≤ $88 | **0.00** | 0.67 | **0.09** | $96 ✗ |
| 격리 · c5 | Sabonis ≤ $26 | **1.00** | 0.73 | 0.64 | **$68 ✗** |

## 🔴 세 모델이 0.00 / 0.6 / 0.1 로 갈립니다 — **그 사실 자체가 결론입니다**

| | 가정 | Jokić ≤ $88 |
|---|---|---|
| ① 균등 | 추정 구간이 지지집합이다 | **0.00** — 반증됨(4건이 실제로 일어남) |
| ② 국소 잔차비 | 우리 순위 오차 = 가격 오차 | **0.67** — `docs/08` §0 오염(브레이크아웃) |
| ③ 엘리트 대조군 | 작년 비쌌던 선수의 연도 간 이동 | **0.09** — n=11 · 위쪽 꼬리는 우리 재평가 |

**어느 하나도 단독으로는 믿을 수 없습니다.** ②는 약 7배 부풀리고 ①은 0으로 죽입니다.
진실은 대략 **0.1~0.2** 구간으로 보이고, 그 근거는 ③이 유일하게 오염 없는 부분집합인데
n=11이라는 것입니다.

→ **트리거를 단일 확률로 설계하면 안 됩니다.** 판단표는 "이 조건이 참이면 이 코어"라는
이진 분기인데, 그 조건의 발동 확률이 모델에 따라 0.00~0.67로 갈립니다. 어떤 모델을
믿든 **c2·c3는 얇습니다** — ②조차 "우리 순위가 틀렸을 확률"을 재고 있을 뿐입니다.

## 🔴 c5의 가격 게이트는 아무것도 거르지 않습니다 — 이건 지시서가 맞았습니다

`Sabonis ≤ $26` · 시장 추정 $15-23 → 균등 P = 1.00. 그런데 **작년 실낙찰가는 $61
(스케일 후 $68)** 입니다. 즉 이 조건은 **"부상 할인이 이미 시장가에 반영됐다"는 우리 추정을
스스로 다시 확인하는 동어반복**입니다. 할인이 실제로 없으면 게이트는 침묵하고
코어 5는 조건 충족으로 잘못 뜹니다.

c5의 실질 조건은 `cond.desc`의 **"프리시즌에 정상 복귀 확인"** 이고, 그건 가격 트리거가
아닙니다. **사용자 판단(2026-08-26)이 "걍 잘 복귀할듯"으로 기록돼 있어
`league_rules_confirmed.sabonis_haliburton`에 "측정이 아니라 기대"로 남아 있습니다.**
가격 $26이 아니라 **실낙찰가 대비 상대 조건**으로 바꾸는 것이 맞습니다(권고 3).

## c1의 실질 트리거는 Hali 하나 — 지시서가 맞았습니다

`KAT ≤ $50`은 시장 $41-49라 균등 P = 1.00으로 아무 역할이 없습니다.
결합 확률 0.52는 사실상 Hali 조건 하나가 만듭니다.

## B-1b · 불변식 I26 신설 — **다만 등급은 지시와 다릅니다**

지시는 "P가 0 또는 1이면 **위반**"이었습니다. 그대로 넣으면 **18건이 위반으로 뜨고
그중 4건은 작년에 실제로 일어난 사건**입니다. 검증기가 사실과 반대되는 것을 강제하게
됩니다. 그래서 이렇게 넣었습니다:

```
위반(err)  판단표 가격 조건이 **균등 AND 국소 실측 둘 다** 발동 불가 / 둘 다 항상 참
           → 죽은 분기 · 게이트 역할 없는 조건. 현재 0건.
경고(warn) 철수가·과열선이 두 모델 모두에서 비구속(아무 일도 안 일어남). 현재 3건:
           Gobert 철수가 >$18 · Zubac 과열선 >$49 · Şengün 과열선 >$84
```

두 모델이 **동시에** 불가라고 할 때만 잡으므로 오탐이 구조적으로 막힙니다.
`validate.py` **위반 0건 유지** — A의 파이프라인을 막지 않습니다.

음성 테스트 2건을 `tests/negative_tests.py`에 넣었습니다(**40/40 통과**):
- `I26a` 판단표 조건을 절대 불가로 주입(`Jokić ≤ $1`) → 잡힘
- `I26b` 항상 참으로 주입(`my_max`까지 함께 올림) → 잡힘
  · 작성 중 두 번 실패했습니다: ① 규칙 2개짜리 행(c1)을 고르면 결합 확률이 곱이라
  한쪽만 1.0으로 만들어도 발화하지 않고, ② 임계값만 올리면 I10(`임계값 ≤ my_max`)이
  먼저 걸립니다. 둘 다 테스트 쪽 결함이었고 주석에 남겼습니다.

## B-1c · 권고 (결정은 사용자)

### c2 · c3 — 죽지 않았으나 얇다

| 선택지 | 대가 |
|---|---|
| **① 유지** (아무것도 안 함) | 국소 실측 0.59~0.67을 신뢰한다는 뜻. 그 확률의 정체는 "우리 순위가 틀렸을 확률"이고, 작년 실적은 둘 다 ✗였다. 드래프트 당일 두 코어가 안 열려도 기본값 c6로 흐르므로 **손해는 "코어 2개가 장식"** 에 그친다 |
| **② `my_max` 상향으로 살린다** | Jokić +$5 · SGA +$2면 `obtainable`이 된다(B-2 참조). **가장 값싼 선택지**지만 실지출이 나가는 방향이고 B-2의 구조 문제와 함께 봐야 한다 |
| **③ 5코어 체제로 축소** | 판단표가 단순해지지만 스타 집중형(c2)·효율 앵커형(c3) 대안이 사라진다. 두 코어의 최소 승률은 25.7%·31.1%로 원래 하위권이라 손실은 크지 않다 |
| **④ 절대 달러 → 상대 조건** | 예: `Jokić가 시장 하단($93) 이하에 낙찰`. 시장이 통째로 뜨거워도 조건이 살아 있고, 툴의 `시장가 보정`과도 자연스럽게 맞물린다. **구현 비용이 가장 크다**(판단표 스키마·툴 판정·I26 전부 상대값을 알아야 함) |

**권고: ②를 B-2 결정과 묶어서 처리하고, 그게 안 되면 ④.** ①은 문제를 남기고 ③은
정보를 버립니다.

### 🔴 ②가 생각보다 강합니다 — 획득 가능성뿐 아니라 **트리거 확률도 함께 움직입니다**

트리거가 얇은 이유는 시장이 변덕스러워서가 아니라 **임계값이 시장 하단보다 아래에
걸려 있어서**입니다. `my_max`를 시장 하단까지 올리고 트리거를 거기 맞추면,
엘리트 대조군 기준으로:

    P(Jokić ≤ $88   · 현행 임계값)   = 1/11 = 0.09
    P(Jokić ≤ $93   · 시장 **하단**)  = 2/11 = 0.18
    P(Jokić ≤ $97   · 시장 **중간**)  = 5/11 = 0.45
    (SGA도 동일: $72 → 0.09 · $81 → 0.18 · $85 → 0.45)

⚠️ **여기서 제 앞선 요약을 정정합니다.** "$7이면 절반이 된다"는 과장입니다 —
두 개는 **다른 안**이고 비용이 다릅니다. I10이 `임계값 ≤ my_max` 를 요구하므로
임계값을 올리려면 `my_max` 를 먼저 그만큼 올려야 하기 때문입니다.

| 안 | my_max 인상 | 트리거 임계값 | P (엘리트 대조군) |
|---|---|---|---|
| **하단안** | Jokić +$5 · SGA +$2 = **+$7** | 시장 하단($93 · $81) | 0.09 → **0.18** |
| **중간안** | Jokić +$9 · SGA +$6 = **+$15** | 시장 중간($97 · $85) | 0.09 → **0.45** |

하단안은 `obtainable`을 켜는 최소 비용이고, **동전던지기가 되려면 중간안이 필요합니다.**
어느 쪽이든 판단표 2행이 되살아나는 값으로는 싼 편입니다 — 다만 중간안은
"시장 중간값을 다 주고 산다"는 뜻이라 예비비를 함께 봐야 합니다(c2 $13 · c3 $10).

### c5 — 가격 게이트를 실질 조건으로 교체

`Sabonis ≤ $26`은 동어반복입니다. **`실낙찰가 ≤ 시장 하단`** 또는 아예 가격 조건을
빼고 `cond.type = "manual"`(건강 확인)만 남기는 쪽을 권합니다. 후자가 정직합니다 —
지금도 실질 조건은 그것뿐입니다.

### 🔴 경고 등급 항목이 **다른 계층의 위반**을 만들 수 있습니다 (39차 · Duren 사례)

I26 표에 `과열선 Duren > $15` 가 **균등 P=1.00(항상 참)** 으로 떠 있었습니다. 저는 이것을
"판단표 행이 아니므로 위반이 아니라 경고"로 등급 판정하고 넘겼습니다. 등급으로는 맞습니다.

**놓친 것은 2차 효과입니다.** Duren의 과열선 $15가 그의 시장 하단 $24보다 낮아
**상시 점등**돼 있었고, 우선 0의 발동 조건은 "저가 센터 6명 중 **2명 이상** 과열"인데
한 명이 항상 켜져 있으니 **실질 문턱이 1명**이었습니다. 즉 판단표 우선 0이 설계보다
두 배 쉽게 열립니다 — 경고 하나가 다른 계층의 **판정 규칙을 무력화**한 것입니다.

원인은 `expected_2026_27`의 근거가 *"작년 실측 $10 × 1.117"* 이었던 것이고,
Duren은 그 사이에 올라온 선수입니다 — **이 문서 앞부분에서 인정한 `docs/08` §0 오염**
('작년 가격을 개별 선수 판정에 씀')이 네 번째로 나타난 자리입니다.

A가 `expected_2026_27`을 시장 중간값 기반으로 통일해 해소했습니다(과열선 $15 → **$34**,
발동 확률 1.00 → 0.15). **비구속 임계값 3건 → 1건**으로 줄었습니다.

> **교훈**: 경고 등급 항목을 볼 때 *"이 값이 다른 계층의 조건식에 들어가는가"* 를 물을 것.
> 이 표(트리거 확률)를 **판단표와 겹쳐 읽어야** 보입니다. 표 하나만으로는 안 보입니다.

⚠️ 부수 확인: 재산정 후 Duren 과열선 $34는 이제 **균등 P=0.00**(시장 $24-30)으로
반대 극단에 갔지만, 실측 0.15라 I26이 잡지 않습니다 — **두 모델 AND 설계**가
여기서 오탐을 막았습니다.

### 비구속 임계값 (39차 평가 결정 ②) — 재산정 후 **3건 → 1건**

`Gobert 철수가 >$18` · `Zubac 과열선 >$49` · `Şengün 과열선 >$84` 는 어떤 모델에서도
발동하지 않습니다. 과열선 2건은 `기대치 × 1.25` 공식 출력이라 **무해**합니다.

`Gobert 철수가 $18`은 다릅니다 — "이 값을 넘으면 안 산다"는 의사결정선인데 그가 $18에
갈 일이 없어 **아무 보호도 하지 않습니다.**

🔴 **그런데 $13으로 내리면 안 됩니다.** 철수가와 과열선은 20차에 **의도적으로 분리한
2계층**이고(전자는 "얼마까지 부를까", 후자는 "시장이 뜨거운가"), 내리면 두 개념이 도로
합쳐집니다. 그게 원래 병이었습니다.

**진짜 위험은 반대쪽입니다.** Gobert가 **$13~18 구간**(과열선은 넘었는데 철수가는 안 넘음)에
가면 계획은 "계속 사라"고 말합니다 — 시장이 뜨겁다고 판정해 놓고 매수를 계속하는 겁니다.
그것이 "$18이 비구속"이라는 사실의 실제 의미입니다.

→ **이 선수의 실질 보호선은 철수가 $18이 아니라 과열선 $13입니다.**
데이터에 `binding: false`를 명시하고 **툴에서 보이게** 해야 합니다 — 드래프트 당일
사용자가 "철수가 $18"을 보고 보호받는다고 믿는 것이 가장 나쁩니다.
(데이터·툴은 A 소유 · 요청 전달함)

---

# B-2 · `my_max` 상단 압축 진단

## 재현 — 총량이 아니라 **분포**입니다

지시서는 "상위 126명 합 시장 $2,772 vs my_max $2,655(4.2% 작음)"라고 했는데,
현재 HEAD에서 재현하면 **$2,754 vs $2,772 = 99.4%** 로 총량 차이는 사실상 없습니다
(37차에 `my_max`를 17명 올린 뒤라 지시서 수치와 갈립니다). **총량은 문제가 아닙니다.**

문제는 분포이고, 그건 지시서가 정확했습니다:

| 시장 순위 | n | my_max 합 | 시장중간 합 | **비율** | 획득 가능 |
|---|---|---|---|---|---|
| 1~10 | 10 | $588 | $796 | **0.739** | **0 / 10** |
| 11~30 | 20 | $737 | $893 | 0.825 | 7 / 20 |
| 31~60 | 30 | $744 | $663 | 1.122 | 24 / 30 |
| 61~126 | 66 | $685 | $420 | **1.633** | 65 / 66 |

**단조 증가입니다.** 상단을 26% 눌러앉히고 하단을 63% 부풀립니다.
같은 총액을 상위에서 빼 하위에 뿌린 모양이고, 이는 `value_model.py`가
**양수 z 비중대로 총액을 선형 배분**하는 구조와 정확히 일치합니다 — z는 대략 정규인데
실제 옥션 가격은 순위에 대해 볼록이므로, 선형 배분은 볼록성을 잃고 상단을 깎습니다.

### 의도된 할인과 구분했습니다 (지시서에 없던 확인)

상위권에는 `tag=burn`(13캣에서 일부러 낮게 보는 선수 — Luka 0.60 · Ant 0.53 ·
Giannis 0.38)이 섞여 있어 "압축"이 아니라 **의도된 판단**일 수 있습니다. 제외하고 봐도:

    1~10  0.739 → 0.784      31~60   1.122 → 1.170
    11~30 0.825 → 0.900      61~126  1.633 → 1.679

**단조 패턴이 그대로 남습니다.** burn 태그로는 설명되지 않습니다.

### 독립 확인 — 작년 실측 잔차비가 같은 말을 합니다

B-1에서 만든 잔차비는 `my_max`를 전혀 쓰지 않는데도 같은 결론을 냅니다:
우리 z 상위 1~10위의 실낙찰가는 사다리 예측의 **0.50배**, 71위 이하는 **3.75배**.
**두 개의 독립 경로가 같은 방향을 가리킵니다.**

## 고치면 무엇이 달라지는가

상위 10명 전원을 `obtainable`로 만드는 데 필요한 인상액:

| 선수 | 필요 인상 | | 선수 | 필요 인상 |
|---|---|---|---|---|
| SGA | +$2 | | Wembanyama · Cade | +$20 |
| Jokić | +$5 | | Maxey | +$22 |
| Jalen Johnson | +$8 | | Luka | +$31 |
| Barnes | +$13 | | Ant Edwards | +$32 |
| Tatum | +$17 | | **합계** | **+$170** |

- **Jokić +$5 · SGA +$2 만으로 c2·c3가 살아납니다**(B-1 권고 ②). 비용 대비 효과가
  압도적입니다 — 판단표 7행 중 2행이 이 $7에 걸려 있습니다.
- Luka·Ant·Giannis는 `burn` 판단이 별도로 있으므로 **인상 대상이 아닙니다.**
  압축 교정과 태우기 판단을 섞지 마십시오.

## ⚠️ 하지 않은 것과 그 이유

- **`players.json` 미변경.** `my_max` 상향은 실지출이라 리스크가 비대칭입니다
  (HANDOFF 열린 항목). 37차에 대칭 상향을 한 번 적용했고, 그때 **규칙이 반복 적용하면
  발산한다**는 것도 드러났습니다(`gp_adj = my_max × GP/64.7`). 같은 실수를 반복하지
  않으려면 **어떤 규칙으로 얼마나 올릴지를 먼저 정해야** 합니다.
- **승률 재측정 안 함.** 지시서의 "상한 없음 93.2% / $50 88.9%" 표는 A 소유
  (`matchup_sim.py`)라 건드리지 않았습니다. 제 진단은 **가격 분포**에 한정됩니다.

## 권고 — 압축 교정은 `value_model` 층에서

개별 `my_max`를 손으로 올리는 것은 37차에 한 방식이고 발산 위험이 있습니다.
근본 교정은 **배분 함수를 볼록하게 바꾸는 것**입니다 — 예: z 비중 대신
`z^k`(k>1) 비중, 또는 실측 곡선의 순위-가격 형상에 직접 적합. 그러면 상단이 자동으로
복원되고 하단이 눌립니다. 검증은 이미 있습니다: **구간별 비율이 1.0 근처로 평탄해지는가.**

다만 이건 평가 척도(`value_reference`)의 변경이고 `my_max`는 그 위에 서 있으므로,
**바꾸면 174명 전원의 값이 움직입니다.**

## 🔒 NEXT_SEASON — 이 교정은 **시즌 후**에 한다 (39차 평가 결정 ⑤)

**진단은 유효하되 교정 시점은 시즌 후입니다.** 174명을 움직이면 `cat_weights`·코어 계획가·
승률·툴 임베드가 연쇄로 바뀌고, **드래프트(2026-09-05)까지 재검증할 시간이 없습니다.**

이번 시즌에 한 것은 **점 수정 2건뿐**입니다 — Jokić `my_max` 88→97 · SGA 79→85
(중간안 · 사용자 결정 2026-08-26 · A가 적용). 그 외 압축은 **그대로 둡니다.**

> ⚠️ 다음 작업자에게: 이 절을 보고 드래프트 직전에 `value_model`을 고치지 마십시오.
> 시점을 안 적으면 다음 사람이 직전에 손댄다는 것이 이 저장소의 반복 패턴입니다.

---

# B-3 · GP가 **측정치인가 투영치인가** (39차 · 평가 세션 확장 지적)

재현: `python3 tool/gp_provenance.py` · **데이터 무변경**

## 모델의 비대칭

혼합 모델이 GP를 **한 종류로만** 취급합니다. 실제로는 두 종류입니다:

```
Şengün      혼합GP 73.7   ← 그가 실제로 뛴 경기 수                     측정치
Haliburton  혼합GP 73.0   ← 2024-25 경기 수를 2026-27 가용성으로 쓴 것   투영치
```

`cat_model`은 둘을 구분하지 않고 `avail = GP/82`로 같이 씁니다.
`measured_source.seasons`에 정보가 있지만 **평가 경로 어디에서도 이 구분을 쓰지 않습니다**
(`gp_qualified`는 GP 임계값이지 출처가 아닙니다). 즉 **"아킬레스 복귀 시즌에 건강한 73경기"가
다른 선수의 실측 73경기와 같은 무게로** 들어갑니다.

## 🔴 단위 함정 — `weight`는 0~1 비율이 아닙니다

`seasons["2025-26"].weight`는 **GP 기반 원 가중치**입니다(Trae Young: 2025-26 `w=22.5 / GP=15`,
2024-25 `w=76.0 / GP=76`). 두 시즌 가중치 합이 우연히 98~99 근처라 **원값이 퍼센트처럼
보이지만** 그렇게 읽으면 안 됩니다. 정규화가 필요합니다:

    실측 비중 = w(2025-26) / ( w(2025-26) + w(2024-25) )

제가 처음에 `weight < 0.15`로 걸러서 **투영 3명만 잡고 Trae·Sabonis를 놓쳤습니다.**
평가 세션의 목록(23% · 29%)이 맞았고 제 필터가 틀렸습니다.

## 코어 1순위 노출 — **7개 중 4개**

| 코어 | 슬롯 | 선수 | 실측 비중 | 혼합 GP | 등급 | 헤지 선언 |
|---|---|---|---|---|---|---|
| c1 | PG ★앵커 | Tyrese Haliburton | **0.0%** | 73.0 | 투영 | `players.flag` |
| c5 | PG ★앵커 | Tyrese Haliburton | **0.0%** | 73.0 | 투영 | `flag` · `cores.c5` |
| c3 | BN | Damian Lillard | **0.0%** | 58.0 | 투영 | 🔴 **없음** |
| c4 | PG | Trae Young | 22.8% | 62.1 | 혼합-저 | 🔴 **없음** |
| c5 | PF ★앵커 | Domantas Sabonis | 28.9% | 55.2 | 혼합-저 | `cores.c5` |
| c3 | UTIL | Ivica Zubac | 47.4% | 64.8 | 혼합 | 🔴 **없음** |
| c5 | UTIL | Ivica Zubac | 47.4% | 64.8 | 혼합 | `cores.c5` |

**c5는 세 명입니다** — 아킬레스 복귀(Hali)와 반월판 수술 복귀(Sabonis)에 **동시에** 겁니다.
DB 전체로는 투영 3명 · 혼합-저 5명 · 혼합 17명입니다.

## 🔴 헤지가 있어도 **규칙이 아니라 서술문**입니다

Hali의 `flag`에 "Nembhard 헤지 필수", c5 `premise`에 헤지 문구가 있습니다. 그런데 그건
**문자열**이고 검증기가 보지 않습니다 — **다음 재설계에서 조용히 사라질 수 있습니다.**
c1·c3 피벗 재설계가 예정돼 있어 지금이 그 위험 구간입니다.

헤지 선언이 아예 없는 노출 **3건**: `c3 BN Lillard` · `c4 PG Trae Young` · `c3 UTIL Zubac`.

## 이 감사가 답하지 못하는 것

**GP가 실제로 몇일지는 모릅니다.** 아킬레스·반월판 복귀 시즌 표본이 DB에 없습니다.
그래서 이 목록은 *"가정이 어디에 걸려 있는가"* 만 말하고, 판정은 **참을 묻는 시험이 아니라
견고성을 묻는 시험**이어야 합니다:

- 둔감 → *"이 코어는 GP에 둔감하다. 따라서 GP가 몇이든 상향은 안전하다."*
- 민감 → *"이 코어는 GP에 민감하고 그 값은 측정 불가다. 따라서 보수적 값을 유지한다."*

두 결론 다 **"GP가 얼마인지 모른다"를 전제로 성립합니다.** 그것이 이 판정의 강점입니다.
"GP 73이 맞다"는 결론은 이 방법으로 나올 수 없습니다.

## 🔒 c7 기본값 교체 안건은 **소멸했습니다** (39차 · A의 c6 교체 이후)

한때 "무조건 상위인 코어가 c7 하나뿐이니 기본값을 c7로 올리자"는 논의가 있었습니다.
A가 c6를 고친 뒤(`Clingan $12 → Desmond Bane $22`) **그 전제가 사라졌습니다.**

실제 12팀 상대 · 6000시행(`d9ec72d` 재실행 후):

| 코어 | 평균(layer1) | 최저(layer2) |
|---|---|---|
| **c6** | **87.5%** | 71.6% |
| c1 | 86.8% | **72.8%** |
| c7 | 86.4% | 70.3% |

**c7은 3위입니다.** 어느 지표로 봐도 기본값 후보가 아닙니다.

⚠️ 두 지표가 1위를 다르게 가리킨다는 점은 기록해둡니다 — 평균은 c6, 최저는 c1입니다.
38차에 목적함수가 재구조화돼 **`layer1_primary` = 실제 12팀 평균**이고 maximin은
`layer2_robustness`("**선택 기준이 되면 안 된다**")로 격하됐으므로 선택 기준으로는
c6가 맞습니다. 그 구조를 모르고 최저값만 보면 다른 결론이 나옵니다.

> 다음 사람에게: 시즌 후 이 안건을 다시 열려면 **낡은 비교표가 아니라 그때의
> `matchup_sim.json`을 다시 돌려** 확인하십시오. 구현 연쇄는 A 확인 결과 하드코딩
> 제약이 `dt[0]["core"] != "c7"` **한 줄뿐**이라 제가 앞서 추정한 범위
> (`overheat_tiers`·`hot_bigs`·I11 전체)보다 작습니다.

## 🔒 불변식은 넣지 않습니다 — **강제는 이미 있고, 없던 것은 가시성입니다**

> **이 노출들의 강제는 불변식 6이 이미 하고 있습니다**(비앵커 슬롯 대체 후보 2명).
> 이 표는 강제가 아니라 **가시성**을 위한 것이고, 재설계 시 사람이 대조할 목록입니다.

검증했습니다 — 투영/혼합 1순위 슬롯 7개 중 **6개가 이미 실측 대체를 2명씩** 갖고 있습니다:

| 슬롯 | 1순위 (실측 비중) | 실측 대체 |
|---|---|---|
| c1 PG ★ | Haliburton 0.0% | Giddey 53.6% · Fox 63.5% (`on_fail: substitute → Giddey`) |
| c5 PG ★ | Haliburton 0.0% | 동일 |
| c3 BN | Lillard 0.0% | Ayton 73.0% · Queta 64.8% |
| c4 PG | Trae Young 22.8% | Schröder 58.3% · Nembhard 56.8% |
| c3 UTIL | Zubac 47.4% | M.Robinson 84.1% · Ayton 73.0% |
| c5 UTIL | Zubac 47.4% | M.Robinson 84.1% · M.Williams 67.2% |
| **c5 PF ★** | **Sabonis 28.9%** | **🔴 실측 대체 0명** (`on_fail: switch_core → c6`) |

제가 "헤지 선언 없음"으로 표시한 3건(c3 Lillard · c4 Trae · c3 Zubac)이 전부 여기 포함됩니다 —
**선언이 없을 뿐 불변식 6이 구조적으로 강제**하고 있었습니다.

여기에 검사를 하나 더 얹으면 **같은 것을 두 곳에서 검사**하게 되고, 그건 이 저장소가
반복해 당한 「규칙 이중 구현」입니다(39차에 `tool_embed.py`로 없앤 바로 그 형태).
게다가 "헤지"를 **문자열로 찾는 검사**는 단어만 적고 통과하는 경로를 만듭니다 —
「빠뜨려도 조용히 통과한다」의 새 사례가 됩니다.

### ⚠️ 다만 7번째(c5 Sabonis)는 **규칙이 아니라 사실로** 덮여 있습니다

`switch_core → c6`이고 c6는 투영/혼합-저 선수를 쓰지 않습니다(최저 실측 비중 **58.7%**).
그런데 **불변식 13이 검사하는 것은 "목적지가 같은 앵커에 의존하지 않는가"이지
"같은 GP 위험에 노출되지 않는가"가 아닙니다.** 코드를 확인했습니다.

즉 **c6가 깨끗한 것은 현재의 사실이지 규칙이 보장하는 바가 아닙니다.**
c6가 나중에 투영 GP 선수를 쓰도록 재설계되면 c5의 탈출로가 같은 위험에 노출되는데
아무도 잡지 못합니다. 이 표를 그때 다시 돌리는 것이 유일한 방어입니다.

---

## 🔒 NEXT_SEASON 추가 — 지명 풀 126명 경계가 **정렬 타이브레이크로 정해진다** (39차 A · 2026-08-27)

`divergence_rules.pool_names()`는 `-(market_low + market_high) / 2` 로 정렬해 상위 126명을
자릅니다. 그런데 **시장 $1-3 동가가 대량이라 경계 근처는 순서가 사실상 임의**입니다.

실제로 드러난 형태 — 같은 c2 base, 같은 시장가 $1-3, 같은 「우리가 과소평가」 방향인데
한 명만 M6에 걸립니다:

| 선수 | div | my_max | 시장 | 지명 풀 | M6 |
|---|---|---|---|---|---|
| Immanuel Quickley | **+74** | $5 | $1-3 | **안** | 🔴 발동 |
| Collin Gillespie | **+103** | $4 | $1-3 | **밖** | 미발동 |

**괴리가 더 큰 쪽이 안 걸립니다.** 이것은 실질 차이가 아니라 컷 위치의 산물입니다.

### 왜 지금 안 고치는가

풀 정의 위에 `cat_baselines` · `value_model` · `marginal` 이 전부 얹혀 있습니다. 드래프트
8일 전에 126명 집합을 흔들면 기준선이 움직이고 **모든 코어의 캣 판정이 재계산**됩니다.
지금 필요한 것은 그 숫자가 아니라 드래프트 당일 쓸 계획입니다.

### 시즌 후에 볼 것

- 동가 구간에서 결정적 타이브레이크를 쓸 것(예: `value_reference.z_total` 2차 정렬).
  지금은 dict 순서에 의존해 **재현성도 없습니다**.
- 또는 경계를 하드 컷이 아니라 **밴드**로 두고, 밴드 안은 감시 목록으로 뺄 것
  (27차에 `|div| 18~22`에 이미 같은 처방을 했습니다 — 같은 병입니다).
- 어느 쪽이든 **고치기 전에 M6 위반 목록이 몇 건에서 몇 건으로 변하는지 먼저 재고**,
  늘어난 건이 전부 사람 판단을 요구하는지 확인할 것.

기록 위치: `players.json` → `Immanuel Quickley.my_max_basis.pool_boundary_note`

---

## 🔒 NEXT_SEASON 추가 — `cond.market` 을 실제 검사로 승격할지 (39차 A · 2026-08-27)

판단표의 `cond.market`(`"normal"` · `"slightly_hot"`)은 **표시 전용**입니다.
`decideStatus()` 는 이 필드를 보지 않고 `cond.rules`(가격)만 검사합니다.

39차 이전에는 라벨과 툴 표시가 이 필드로 접두어를 만들어 **구현되지 않은 조건을
주장**하고 있었습니다:

```
우선 1  c1  "센터 정상가 + KAT ≤ $50 + Hali ≤ $56"   실제 검사 = KAT·Hali 가격만
우선 3  c3  "센터가 조금 비싸지만 SGA ≤ $85"          실제 검사 = SGA 가격만
```

**라벨과 툴 접두어에서 뺐습니다. 동작은 바꾸지 않았습니다.** 필드는
`cond.display_only` 와 함께 남겨 뒀습니다.

### 왜 지금 검사로 만들지 않는가

- **드래프트 7일 전에 발동 조건을 바꾸는 것**입니다. 판단표는 당일 10초 안에 쓰는 물건이라
  발동 방식이 바뀌면 리허설을 전부 다시 밟아야 합니다
- **센터 시장 판정은 이미 우선 0(c7)이 별도로 합니다** — `hot_bigs` 로 저가 센터 계층의
  과열선 초과를 셉니다. 같은 것을 두 곳에서 판정하면 이 저장소가 반복해 당한
  「규칙 이중 구현」입니다
- 39차에 **충족이 2행 이상이면 각 행에 계획 여유를 띄우도록** 고쳤습니다. 센터가 비싸서
  어떤 코어가 조립 불가가 되면 **그 숫자가 직접 보입니다** — 라벨의 주장보다 강한 신호입니다

### 시즌 후에 볼 것

- `market` 을 `hot_bigs` 와 **같은 판정기**로 계산할 것(과열 카운트 임계). 별도 구현 금지
- 승격하면 c1·c3 의 발동 확률이 내려갑니다 — `docs/11` 발동 확률표를 다시 돌릴 것
- 아니면 필드를 **삭제**하는 것도 정직한 선택입니다. 지금은 아무것도 하지 않는 필드입니다

---

## 🔒 NEXT_SEASON 추가 — 감사 절차 2건 (40차 B · 2026-08-27)

이번 회차에 **같은 형태의 실패를 세 번** 봤습니다. 개별 결함이 아니라 절차의 구멍이라
여기에 남깁니다.

### ① 사실을 기록할 때, 그 사실에 의존하던 문장을 함께 훑을 것

| # | 사실은 이미 기록돼 있었다 | 그런데 |
|---|---|---|
| 1 | 툴 범례가 코드와 다름(`차익` = 중간값인데 범례는 상단) | 두 세션이 코드 대신 **범례를 다시 읽고** 「정의가 둘로 갈렸다」고 결론 |
| 2 | 리허설은 `/tmp/published.html` 을 우선 사용 | 실행 대상을 출력에 찍지 않아 **옛 발행본이 "정상"** 으로 통과 |
| 3 | `cores.json.draft_date`: *"'개막 1~2주 전' 가정은 틀렸다"* | 그 가정에 의존하던 **13곳이 안 고쳐짐**(`docs/05 §6b`) |

셋 다 **사실을 적는 것과 그 사실을 반영하는 것이 다른 일**이라는 같은 모양입니다.
그리고 셋 다 *"검사가 통과했다"* 가 *"무엇을 검사했는가"* 를 확인하지 않은 채 신뢰된 경우입니다.

**제안하는 검사** — 자동화 가능해 보입니다:

- 새 사실(정정·측정·날짜 변경)을 기록할 때, 그 사실이 부정하는 **옛 진술의 키워드**를
  `grep` 으로 저장소 전체에서 한 번 훑는다. 3번은 `"프리시즌"` 한 번이면 13곳이 다 나왔다
- 후보 형태: `validate.py` 에 **금지 문구 사전**을 두는 것. 예를 들어 드래프트 날짜가
  프리시즌보다 앞서는 한 *"프리시즌에 확인"* 이 완화책으로 쓰인 곳을 세어 경고한다
  (불변식이 아니라 `△` 주의 줄로 — 문장 판정이라 오탐이 있을 수 있다)

### ② 외부 세션의 수치를 받을 때 **적용 범위**부터 볼 것

`r_GP = 0.410` 을 받아 *"그러므로 Haliburton 투영 GP = 54.3"* 까지 문서에 적었는데,
**Haliburton 은 그 회귀의 표본에 아예 없었습니다**(2025-26 전체 결장 → GP 쌍이 안 만들어짐).
계수 자체는 옳았고 **적용 대상이 틀렸습니다.**

- 외부 수치를 인용할 때 **① 표본이 무엇인가 ② 이 선수/코어가 그 표본에 있는가** 를 먼저 묻는다
- 표본 밖이면 그 수치는 **모양**(방향·크기 감각)으로만 쓰고 **값으로 쓰지 않는다**
- 저장소 규칙대로 출처와 `미재현` 표기는 이미 하고 있다. 여기에 **적용 범위**를 한 줄 더 붙인다

### ③ 전달받은 값은 **원본 아티팩트에서 다시 뽑을 것** — 메시지를 옮기지 말 것

40차에 두 세션이 **같은 실수를 다섯 번** 했습니다. 형태가 전부 같습니다 —
**한 다리 건넌 출처를 원본으로 취급**한 것입니다.

```
범례를 읽고 코드를 안 봤다              → 「차익 정의가 둘」이라는 없는 결론
r_GP 를 재현 없이 넘겼다                → 표본에 없는 선수에게 적용
옛 값 목록을 메시지에서 옮겨 적었다      → 실제 값 4개가 빠진 색출 목록
리허설이 어느 파일을 읽는지 안 찍었다    → 옛 발행본이 "정상"으로 통과
"Gobert 는 작년 이 리그에 없었다"        → 실제로는 $8 에 팔렸다(발행 전 확인)
```

**실수를 했느냐가 아니라 아티팩트를 여느냐가 갈랐습니다.**

- 사람/세션이 알려준 값은 **인용 전에 원본 파일·커밋에서 다시 뽑는다.** 옮겨 적기 금지
- 그게 반복되는 작업이면 **스크립트로 만든다** — `tests/stale_figures.py` 가 그 예다
  (옛 값 목록을 메시지에서 받지 않고 `git show <ref>` 로 직접 뽑는다)
- 검사를 만들 때는 **무엇을 검사했는지 출력에 찍는다.** 찍지 않는 검사는
  통과해도 아무것도 보장하지 않는다

### ④ **같은 사실이 두 곳에 있으면 대조하는 검사를 함께 넣는다**

40차에 야후 실자격 19명을 `players.json.pos_yahoo` 로 넣었습니다. 넣고 나서야
같은 사실이 이미 `yahoo_eligibility_39` 에 4명분 들어 있다는 걸 알았습니다.
**넣은 그날 2건이 갈라져 있었습니다.**

```
Şengün    pos_yahoo C,PF   ↔ listed C,PF        일치
Bane      pos_yahoo SF,SG  ↔ listed SF,SG       일치
Amen      pos_yahoo PG,SG  ↔ listed PG,SF,SG    🔴  ← 이 2건이 하필
Okongwu   pos_yahoo C      ↔ listed C,PF        🔴     그날 수리의 근거였다
```

이 저장소가 반복해 당한 형태 그대로입니다 — 39차에 `tool_embed.py` 로 하나를 없앤
직후에 새로 하나를 만들었습니다. 이중 보관은 **의도해서 생기지 않습니다.** 새 필드를
넣는 사람은 옛 필드를 모르고, 옛 필드를 넣은 사람은 이미 없습니다.

**그래서 요점은 「만들지 말 것」이 아닙니다** — 그건 이미 여러 번 실패한 규칙입니다.
요점은 **만들었으면 대조 검사를 같이 넣는 것**입니다. `validate.py` I33 이 그것이고,
넣자마자 위 2건을 잡았습니다.

세부 설계에서 갈린 것 두 가지:

- **등급을 경고로 둔다.** 어느 쪽이 맞는지는 사람이 확인해야 합니다. 위반으로 두면
  확인이 끝날 때까지 검증기가 빨간불이라 **다른 위반이 묻힙니다.**
- **진행을 막지 않아도 되는지 먼저 확인한다.** 이번엔 base 7 + 피벗 7 + 대체후보 전부가
  **두 판독 모두에서 유효**했습니다. 그래서 보수적인 쪽(`pos_yahoo`)을 쓰는 비용이 0이었고,
  그 사실을 확인했기 때문에 경고로 둘 수 있었습니다. 확인 없이 경고로 두는 것은 다른 얘기입니다.

같은 검사에 **`overheat_at` ↔ 계층 `overheat_margin`** 대조도 함께 넣었습니다.
`low_cost_center` 가 「기대치 × 1.4 (최소 +$3)」라고 선언해 놓고 그 계층 6명 전원이
실제로는 **× 1.25** 였습니다 — 데이터가 아니라 **설명문**이 낡은 경우이고,
`name_big` 과 같은 배율이라 **계층 간 배율 차이가 실제로는 없었습니다.**

### ⑤ 「틀린 숫자를 고치는 패치」도 낡는다

39차에 c6·c1 피벗 서술의 빅맨 예산이 틀린 것을 잡아 정정 패치를 썼습니다.
c1 을 `$96 → $78` 로 고치는 패치였는데, 적용하기 전에 40차 재조립이 일어나
**실제가 $82** 가 됐습니다. 패치를 그대로 돌렸으면 틀린 값을 다른 틀린 값으로
바꿀 뻔했습니다(적용 직전 데이터 대조로 잡았습니다).

- **정정 패치에 하드코딩한 수치는 적용 직전에 데이터에서 다시 뽑는다.**
- 더 나은 답은 **검사로 만드는 것**입니다. `I35` 는 피벗 서술의 「빅맨 … $NN」 형태만
  좁게 보고 실제 계산값과 대조합니다. 서술문 전체를 검사할 수는 없지만,
  **두 번 갈라진 자리**는 검사할 수 있습니다.
